---
phase: 04-prose-quality-discussion
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [thesis/main.tex]
autonomous: true
requirements: [WRIT-02, WRIT-03, WRIT-04]
must_haves:
  truths:
    - "Abstract is 250-300 words with 6-element structure: problem, approach, finding 1, finding 2, validation, future direction"
    - "All 6 canonical terms are consistently used: segmentation mask, template-approval planner, image-space midpoint planner, BEV corridor, design iteration, teacher-student framework"
    - "All figure and table captions answer: what is shown, under what conditions, what is the key takeaway"
  artifacts:
    - path: "thesis/main.tex"
      provides: "Rewritten Abstract, standardized terminology, self-contained captions"
      contains: "template-approval planner"
  key_links:
    - from: "Abstract"
      to: "Introduction contributions"
      via: "Key numbers matching contribution evidence"
      pattern: "14.3.*421.*template-approval"
    - from: "Terminology in Ch.3-6"
      to: "Canonical definitions in System Design"
      via: "Consistent term usage"
      pattern: "template-approval planner"
---

<objective>
Rewrite the Abstract to 250-300 words with prescribed structure. Standardize 6 canonical terms across the entire document. Make all figure/table captions self-contained.

Purpose: The Abstract is the first (and sometimes only) thing reviewers read. Terminology inconsistency undermines credibility. Non-self-contained captions frustrate figure-scanning readers.

Output: thesis/main.tex with compressed Abstract (~250-300 words), standardized terminology, and improved captions.
</objective>

<execution_context>
@C:/Users/miji0000/Desktop/thesis_prep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miji0000/Desktop/thesis_prep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prose-quality-discussion/04-CONTEXT.md
@.planning/phases/04-prose-quality-discussion/04-RESEARCH.md
@.planning/phases/04-prose-quality-discussion/04-01-SUMMARY.md

<interfaces>
<!-- Current Abstract: lines 178-191, ~378 words across 5 paragraphs -->
<!-- Target: 250-300 words, 6-element structure per D-07 -->

<!-- Terminology inventory from 04-RESEARCH.md lines 180-193 -->
Canonical terms and variants to replace:
  1. "segmentation mask" — replace "sidewalk mask" (3 instances), "binary sidewalk mask" (1)
  2. "template-approval planner" — replace "template arc planner" (7+ instances)
  3. "image-space midpoint planner" — verify consistency (0-1 non-canonical)
  4. "BEV corridor" — replace "BEV mask corridor" (0-1), "drivable corridor" (0-1)
  5. "design iteration" — replace "Phase~11" (1 instance, line 1096)
  6. "teacher-student framework" — already consistent outside lit review

<!-- Caption assessment from 04-RESEARCH.md lines 196-219 -->
Captions needing rewrite (missing key takeaway or conditions):
  - Subfigure captions: lines 259, 266, 835, 839, 843
  - Tab resolution_sweep: line 490
  - Tab runtime_configs: line 1160
  - Tab template_eval: line 1092
  - Tab waypoint_turn_eval: line 1182
Already adequate (minor polish at most):
  - Fig scooter_hw (line 269), Fig pipeline (line 402)
  - Tab seg_comparison (line 856/872), Tab planner_comparison (line 960)
  - Fig bev_fragility (line 1068), Tab accepted_run (line 1211)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Abstract and standardize terminology</name>
  <files>thesis/main.tex</files>
  <read_first>
    - thesis/main.tex lines 178-191 (current Abstract)
    - thesis/main.tex lines 232-246 (Introduction contributions — Abstract must cover all 5)
    - .planning/phases/04-prose-quality-discussion/04-RESEARCH.md lines 141-157 (Abstract rewrite pattern with structure and word allocation)
    - .planning/phases/04-prose-quality-discussion/04-RESEARCH.md lines 180-193 (terminology inventory with instance counts)
    - .planning/phases/04-prose-quality-discussion/04-CONTEXT.md lines 29-41 (decisions D-07, D-08, D-09)
  </read_first>
  <action>
  TWO distinct operations on thesis/main.tex:

  **Operation A: Rewrite Abstract (lines 178-191)**

  Replace the current ~378-word Abstract with a new ~250-300 word Abstract following D-07's prescribed 6-element structure. Write fresh (do not edit down — Pitfall 4 in research says starting fresh is easier than compressing).

  Word budget per element:
  - Problem (30 words): Sidewalk navigation for micro-mobility, monocular camera, embedded hardware constraint.
  - Approach (50 words): Modular pipeline progressing through "four design iterations" (mention as phrase, not all names per discretion). Systematic comparison of five planning methods across BEV and image-space domains. Teacher-student segmentation training.
  - Finding 1 (60 words): Image-space midpoint planning dominates BEV. Numbers: 14.3px vs 65.0px lateral center error, 421x speedup, 99.3% BEV failure rate (no valid path). Oracle masks confirm BEV limitation is geometric, not segmentation-related.
  - Finding 2 (40 words): Template-approval planner replaces skeleton-graph. 40.6% heading error reduction, 50% fewer path-source switches. Family reuse hysteresis.
  - Validation (40 words): 1800-frame accepted run at 100% template success, 9.97 FPS. 0% turn containment failure. Full stack at 59 FPS on CPU.
  - Future direction (20 words): One sentence on closed-loop deployment and cross-environment generalization.

  What to cut from current Abstract (per research lines 153-157):
  - Detailed segmentation training description (pseudo-labels paragraph)
  - Full-video replay statistics (22,679 frames, instability reduction)
  - Obstacle detection/GPS/serial protocol sentence (line 189)
  - Specific mask-path alignment percentages (98.5% vs 98.6%)

  The Abstract must mention all 5 contributions in some form (Pitfall 4: no contribution absent).

  Tone: Formal thesis (D-02). Data unheedged. Interpretations minimal in Abstract.

  **Operation B: Standardize terminology across entire document**

  Process each canonical term from D-09 systematically. For each term, search the entire file and replace non-canonical variants. Verify contextual appropriateness of each replacement per Pitfall 3.

  1. "template-approval planner": Replace all "template arc planner" instances with "template-approval planner" EXCEPT: (a) at the first introduction in System Design where give both names: "...a template-approval planner (also known as the template arc planner)..." and (b) in table column headers where space is tight (abbreviate to "Template-Approval" if needed). There are ~7-10 instances to fix.

  2. "segmentation mask": Replace "sidewalk mask" with "segmentation mask" except where "sidewalk" is specifically the semantic meaning (e.g., "the sidewalk mask identifies sidewalk pixels" is fine). Replace "binary sidewalk mask" with "binary segmentation mask". ~4 instances.

  3. "BEV corridor": Replace "BEV mask corridor" or "drivable corridor" with "BEV corridor" where found. ~1-2 instances.

  4. "design iteration": Replace the "Phase~11" reference (line ~1096) with "design iteration~4" or "Iteration~4". ~1 instance.

  5. "image-space midpoint planner": Verify consistency. Fix if any variant found.

  6. "teacher-student framework": No action needed outside lit review per research.

  After all replacements, do a verification grep for each non-canonical variant to confirm zero remaining instances.
  </action>
  <verify>
    <automated>echo "=== Abstract word count ===" && sed -n '/^\\chapter\*{ABSTRACT}/,/^\\newpage/p' thesis/main.tex | wc -w && echo "=== template arc planner (should be 0-1) ===" && grep -c "template arc planner" thesis/main.tex && echo "=== template-approval planner ===" && grep -c "template-approval planner" thesis/main.tex && echo "=== sidewalk mask (should be 0-2 contextual) ===" && grep -c "sidewalk mask" thesis/main.tex</automated>
  </verify>
  <acceptance_criteria>
    - Abstract word count is between 250 and 300 (count words between \chapter*{ABSTRACT} and \newpage)
    - grep "template arc planner" thesis/main.tex returns 0-1 matches (only at first-introduction dual-name)
    - grep "template-approval planner" thesis/main.tex returns 7+ matches
    - grep "sidewalk mask" thesis/main.tex returns 0-2 matches (only contextually appropriate uses)
    - grep "Phase.11" thesis/main.tex returns 0 matches (replaced with design iteration)
    - Abstract contains the numbers: 14.3, 65.0, 421, 99.3, 40.6, 0.946, 59 (key evidence)
  </acceptance_criteria>
  <done>Abstract is 250-300 words with all 5 contributions represented. All 6 canonical terms standardized throughout the document. No non-canonical variants remain except contextually appropriate uses.</done>
</task>

<task type="auto">
  <name>Task 2: Make all figure and table captions self-contained</name>
  <files>thesis/main.tex</files>
  <read_first>
    - thesis/main.tex — scan all \caption{} commands (grep for locations)
    - .planning/phases/04-prose-quality-discussion/04-RESEARCH.md lines 196-219 (caption assessment: which need rewriting, which are adequate)
    - .planning/phases/04-prose-quality-discussion/04-RESEARCH.md lines 286-299 (self-contained caption pattern example)
    - .planning/phases/04-prose-quality-discussion/04-CONTEXT.md lines 32-33 (decision D-08)
  </read_first>
  <action>
  Review and improve all \caption{} commands in thesis/main.tex per D-08 (WRIT-02). Each caption must answer three questions: (1) What is shown? (2) Under what conditions? (3) What is the key takeaway?

  For subfigure captions (lines 259, 266, 835, 839, 843): the parent caption handles conditions and takeaway; subfigure captions identify the specific panel content. These are already structured this way — verify and add brief context if missing.

  **Captions requiring substantive rewrite** (per research assessment):

  1. Tab resolution_sweep (line ~490): Currently "SegFormer-B0 inference latency and throughput at various input resolutions (CPU-only)." ADD: key takeaway about the accuracy-latency trade-off and which resolution was selected. Example addition: "The 640x360 resolution provides the best balance of boundary detail and real-time feasibility."

  2. Tab template_eval (line ~1092): Currently "Template arc planner vs. skeleton-graph baseline on 220 frames (calibrated June clip, detection disabled)." REWRITE to: (a) use canonical term "template-approval planner", (b) add key takeaway: 40.6% heading error reduction and 50% fewer path-source switches, (c) specify dataset: VID_017 calibrated segment.

  3. Tab runtime_configs (line ~1160): Currently "System-level runtime under different configurations (4,407-frame profiled run, 512x288 input)." ADD: key takeaway about the image-space architecture achieving 59 FPS vs 2.4 FPS for BEV.

  4. Tab waypoint_turn_eval (line ~1182): Currently "Waypoint-turn planner evaluation: scheduled right-intent window, 300 frames." ADD: key takeaway about 0% containment failure and conditions (VID_017, stride~4).

  **Captions needing minor polish only** (already mostly self-contained):

  5. Fig scooter_hw (line ~269): Already good. Verify "what/conditions/takeaway" structure.
  6. Fig pipeline (line ~402): Already good. Verify.
  7. Tab seg_comparison (line ~856/872): Already good. Ensure uses "segmentation mask" not "sidewalk mask".
  8. Tab planner_comparison (line ~960): Already good. Verify canonical terminology.
  9. Fig bev_fragility (line ~1068): Already good. Verify.
  10. Tab accepted_run (line ~1211): Already good. Verify.

  **Other captions:** Scan remaining captions (subfigures at 835/839/843, qualitative comparison at 888, full-video replay at 899, iteration progression at 934, planner bar charts at 981, BEV vs image-space at 990, oracle comparison at 1001, skeleton pipeline at 1028, BEV path reliability at 1051, runtime merged at 1131/1152). For each, verify self-containment or add brief context. Most of these were created during Phase 3 and should be reasonably self-contained.

  Use canonical terminology in ALL captions (template-approval planner, segmentation mask, BEV corridor, design iteration).

  Do NOT change table content or figure references — only caption text.
  </action>
  <verify>
    <automated>echo "=== Caption count ===" && grep -c "\\\\caption{" thesis/main.tex && echo "=== Captions with template arc (should be 0) ===" && grep "caption.*template arc" thesis/main.tex | wc -l && echo "=== template_eval caption has 40.6 ===" && grep -A2 "caption.*template" thesis/main.tex | grep -c "40.6"</automated>
  </verify>
  <acceptance_criteria>
    - All captions use canonical terminology (grep "caption.*template arc" returns 0)
    - Tab template_eval caption mentions "40.6" (key takeaway added)
    - Tab waypoint_turn_eval caption mentions "containment" (key takeaway added)
    - Tab runtime_configs caption mentions "59" or "FPS" (key takeaway added)
    - No caption contains only a bare noun phrase (every caption has a verb)
  </acceptance_criteria>
  <done>All figure and table captions are self-contained (what/conditions/takeaway). Canonical terminology used in all captions. No content changes to tables or figures — only caption text modified.</done>
</task>

</tasks>

<verification>
1. Abstract is 250-300 words with 6-element structure (problem, approach, finding 1, finding 2, validation, future)
2. All 5 contributions are referenced in the Abstract
3. Zero "template arc planner" instances outside of first-introduction dual-name
4. Zero "Phase~11" references (replaced with design iteration)
5. All captions answer what/conditions/takeaway
6. LaTeX compiles without errors
</verification>

<success_criteria>
- Abstract compressed from ~378 words to 250-300 words with all key evidence numbers
- 6 canonical terms standardized across entire document (~15-20 replacements total)
- Captions improved with key takeaways added to 4 substantive rewrites and ~10 minor polishes
- No terminology inconsistencies remain
</success_criteria>

<output>
After completion, create `.planning/phases/04-prose-quality-discussion/04-02-SUMMARY.md`
</output>
