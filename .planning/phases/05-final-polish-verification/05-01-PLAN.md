---
phase: 05-final-polish-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["thesis/main.tex"]
requirements: ["STRUCT-03", "FRNT-01", "FRNT-02"]
autonomous: true
---

# Plan 05-01: Transitions, Front Matter Verification, and Page Count Check

## Objective

Add brief forward-reference transition sentences between all chapters and between major sections within chapters. Verify front matter completeness. Confirm line count is consistent with the 60-80 page target for OU double-spaced format.

## Context

The thesis has 6 chapters across 1393 lines. Phase 4 completed all prose polish. Some inter-chapter transitions already exist (e.g., Ch.2 Summary ends with a forward reference to Ch.3; Ch.3 opening paragraph references Ch.4). Most section-to-section transitions within chapters are missing. The committee member placeholder on the approval page (line 134) and in Acknowledgments (line 155) stays per D-02.

### Existing Transitions (verified by reading main.tex)

| Location | Status | Notes |
|----------|--------|-------|
| Ch.1 -> Ch.2 | EXISTS | Thesis Organization section (line 249) describes all chapters |
| Ch.2 -> Ch.3 | EXISTS | Summary and Research Gap ends with forward ref to Ch.3 (line 385) |
| Ch.3 -> Ch.4 | PARTIAL | Ch.4 opening paragraph (line 774) mentions 5 claims but Ch.3 has no closing forward ref |
| Ch.4 -> Ch.5 | MISSING | No transition from last Evaluation section to Discussion |
| Ch.5 -> Ch.6 | MISSING | No transition from Threats to Validity to Conclusion |
| Within Ch.2 sections | MISSING | 8 lit review sections flow into each other without forward references |
| Within Ch.3 sections | PARTIAL | Ch.3 opening paragraph provides a roadmap but individual sections lack transitions |
| Within Ch.4 sections | MISSING | Claim sections end with Conclusion subsections but no forward refs to next claim |
| Within Ch.5 sections | MISSING | Discussion sections flow without explicit transitions |

## Tasks

### Task 1: Add inter-chapter transition sentences

<read_first>
- `thesis/main.tex` lines 760-775 (end of Ch.3 Software Architecture -> Ch.4 opening)
- `thesis/main.tex` lines 1249-1260 (end of Temporal Smoothing Evaluation -> Ch.5 Discussion opening)
- `thesis/main.tex` lines 1338-1355 (end of Threats to Validity -> Ch.6 Conclusion opening)
</read_first>

<action>
Add a single forward-reference sentence at the end of each chapter, just before the next `\chapter` command. Three additions:

1. **End of Ch.3 (after Software Architecture section, ~line 767):**
   Add after the last paragraph of Software Architecture, before the `% ===` separator:
   ```
   The experimental evaluation of this pipeline---testing the five planning methods, characterizing BEV fragility, and validating the complete system on extended video---is the subject of Chapter~\ref{ch:evaluation}.
   ```

2. **End of Ch.4 (after Temporal Smoothing Evaluation section, ~line 1253):**
   Add after the temporal smoothing paragraph, before the `% ===` separator:
   ```
   Chapter~\ref{ch:discussion} interprets these experimental findings, examines why image-space planning outperforms BEV, and characterizes the conditions under which the approach fails.
   ```

3. **End of Ch.5 (after Threats to Validity section, ~line 1345):**
   Add after the Construct validity paragraph, before the `% ===` separator:
   ```
   Chapter~\ref{ch:conclusion} summarizes the five contributions, maps each to its supporting evidence, and outlines directions for future work.
   ```
</action>

<acceptance_criteria>
- [ ] Each of Ch.3, Ch.4, and Ch.5 ends with exactly one forward-reference sentence pointing to the next chapter
- [ ] Sentences use `\ref{ch:...}` cross-references (not hardcoded chapter numbers)
- [ ] Sentences are brief (one sentence each, no bridging paragraphs)
- [ ] Ch.1->Ch.2 and Ch.2->Ch.3 transitions already exist and are left unchanged
</acceptance_criteria>

### Task 2: Add intra-chapter transition sentences within Ch.2 (Background)

<read_first>
- `thesis/main.tex` lines 280-390 (all 8 lit review sections + Summary)
- Focus on the last paragraph of each section to see how it ends
</read_first>

<action>
Add a single forward-reference sentence at the end of each of the 8 literature review sections (before the next `\section`). These should be brief, linking the current topic to the next. Add to these section endings:

1. **End of Sec 2.1 (Autonomous Navigation, before line 289):**
   ```
   The segmentation models that underpin these navigation systems are reviewed next.
   ```

2. **End of Sec 2.2 (Semantic Segmentation, before line 301):**
   ```
   Once a drivable mask is obtained, projecting it into a metric coordinate frame enables distance-based planning; the next section reviews bird's-eye view methods for this purpose.
   ```

3. **End of Sec 2.3 (BEV Projection, before line 313):**
   ```
   With a metric occupancy representation available, the next section reviews the path planning algorithms that operate on it.
   ```

4. **End of Sec 2.4 (Path Planning, before line 325):**
   ```
   Distance transform methods offer an alternative to graph-based planning and are reviewed in the following section.
   ```

5. **End of Sec 2.5 (Distance Transform, before line 334):**
   ```
   A related class of methods extracts paths through topological skeletonization, reviewed next.
   ```

6. **End of Sec 2.6 (Skeletonization, before line 345):**
   ```
   The segmentation quality that feeds these planning methods depends on the training strategy; the next section reviews teacher--student approaches for training compact models.
   ```

7. **End of Sec 2.7 (Teacher-Student, before line 357):**
   ```
   Deploying these trained models under real-time constraints on embedded hardware introduces additional challenges, reviewed in the following section.
   ```

8. **End of Sec 2.8 (Embedded Perception, before line 369):**
   ```
   The following section synthesizes the gaps identified across these eight areas of prior work.
   ```

Each sentence goes on its own line, separated by a blank line from the preceding paragraph and before the next `\section` command.
</action>

<acceptance_criteria>
- [ ] All 8 literature review sections (2.1 through 2.8) end with a one-sentence forward reference
- [ ] No transition sentences are duplicated or contradict existing text
- [ ] The Summary and Research Gap section (Sec 2.9) is NOT modified (it already has a forward ref to Ch.3)
</acceptance_criteria>

### Task 3: Add intra-chapter transition sentences within Ch.3 (System Design)

<read_first>
- `thesis/main.tex` lines 406-770 (all System Design sections)
- Focus on the last 2-3 lines before each new `\section` command
</read_first>

<action>
Add a brief forward-reference sentence at the end of each major section in Ch.3. The chapter has 12 sections (Hardware through Software Architecture). Add transitions between these major topic blocks:

1. **End of Hardware Platform (before Segmentation section ~line 415):**
   ```
   These hardware constraints directly shape the segmentation strategy described next.
   ```

2. **End of Segmentation Module (after Training Progression subsection, before Resolution Trade-Off ~line 479):**
   ```
   The interaction between input resolution and inference speed is characterized in the following section.
   ```

3. **End of Resolution Trade-Off (before BEV Projection ~line 514):**
   ```
   With the segmentation resolution established, the next section describes how the resulting mask is projected into bird's-eye view coordinates.
   ```

4. **End of BEV Projection (before BEV Mask Refinement ~line 529):**
   ```
   The raw BEV projection requires morphological refinement before downstream planning, described next.
   ```

5. **End of BEV Mask Refinement (before Path Planning Methods ~line 546):**
   ```
   The refined BEV mask and image-space mask serve as input to the five planning methods described in the following section.
   ```

6. **End of Path Planning Methods (after Turn Containment, before Temporal Smoothing ~line 635):**
   ```
   The raw planner outputs are temporally filtered to reduce frame-to-frame oscillation, as described next.
   ```

7. **End of Temporal Smoothing (before Object Detection ~line 658):**
   ```
   Beyond path stability, the system must also detect and respond to dynamic obstacles in the scene.
   ```

8. **End of Object Detection (after Speed Modulation, before GPS Navigation ~line 684):**
   ```
   Obstacle-aware speed modulation integrates with the GPS waypoint navigation system described next.
   ```

9. **End of GPS Navigation (before Steering and Speed ~line 698):**
   ```
   The GPS intent conditions the planner selection; the resulting path is converted to steering and speed commands in the following section.
   ```

10. **End of Steering and Speed (before Serial Command Protocol ~line 713):**
    ```
    These computed commands are transmitted to the scooter hardware through the serial protocol described next.
    ```

11. **End of Serial Command Protocol (before Safety Mechanisms ~line 723):**
    ```
    All commanded actions pass through the safety gate system described in the following section.
    ```

12. **End of Safety Mechanisms (before Evaluation Metrics ~line 738):**
    ```
    The metrics used to evaluate this pipeline are defined next.
    ```

13. **End of Evaluation Metrics (before Software Architecture ~line 763):**
    ```
    The software architecture that implements these components is summarized in the final section of this chapter.
    ```

Place each sentence on its own line, with a blank line before it and a blank line after (before the next section/separator comment).
</action>

<acceptance_criteria>
- [ ] All 13 section boundaries within Ch.3 have a one-sentence forward reference
- [ ] No transition alters or contradicts the chapter's opening roadmap paragraph
- [ ] Transitions are factual and brief (no interpretive claims)
</acceptance_criteria>

### Task 4: Add intra-chapter transition sentences within Ch.4 (Evaluation)

<read_first>
- `thesis/main.tex` lines 914-926 (end of Claim 1 Conclusion -> Claim 2 opening)
- `thesis/main.tex` lines 1031-1042 (end of Claim 2 Conclusion -> Claim 3 opening)
- `thesis/main.tex` lines 1072-1083 (end of Claim 3 Conclusion -> Claim 4 opening)
- `thesis/main.tex` lines 1111-1120 (end of Claim 4 Conclusion -> Claim 5 opening)
- `thesis/main.tex` lines 1243-1256 (end of Claim 5 Conclusion -> Temporal Smoothing)
</read_first>

<action>
Add a brief forward-reference sentence at the end of each claim's Conclusion subsection (before the next `\section`), and after Experimental Setup:

1. **End of Experimental Setup section (before Claim 1 ~line 823):**
   After the Naive Baseline subsection ends, add:
   ```
   With the evaluation framework established, the following five sections examine each claim in turn, beginning with segmentation quality.
   ```

2. **End of Claim 1 Conclusion (before Claim 2 ~line 920):**
   ```
   With segmentation quality established, the next section examines how planning domain choice affects path accuracy and latency.
   ```

3. **End of Claim 2 Conclusion (before Claim 3 ~line 1039):**
   ```
   The following section investigates the root cause of BEV planning fragility in greater detail.
   ```

4. **End of Claim 3 Conclusion (before Claim 4 ~line 1080):**
   ```
   Given the limitations of skeleton-based path discovery identified above, the next section evaluates the template-approval alternative.
   ```

5. **End of Claim 4 Conclusion (before Claim 5 ~line 1117):**
   ```
   The final claim section integrates these individual findings into a system-level evaluation of sustained real-time operation.
   ```

6. **End of Claim 5 Conclusion (before Temporal Smoothing ~line 1249):**
   ```
   One remaining component---temporal smoothing---is evaluated separately below.
   ```
</action>

<acceptance_criteria>
- [ ] All 5 claim-to-claim boundaries plus the setup-to-claim and claim-to-smoothing boundaries have transitions
- [ ] Transitions use cross-referencing language consistent with the claim-evidence-conclusion structure
- [ ] No transition reveals conclusions of the next section (forward-looking, not spoiling)
</acceptance_criteria>

### Task 5: Add intra-chapter transitions within Ch.5 (Discussion)

<read_first>
- `thesis/main.tex` lines 1256-1345 (all Discussion sections)
- Focus on the last lines of each section
</read_first>

<action>
Add brief forward-reference sentences between the 7 Discussion sections:

1. **End of Interpretation of Key Findings (before Why Image-Space ~line 1268):**
   ```
   The geometric explanation for this image-space advantage is examined in the following section.
   ```

2. **End of Why Image-Space Outperforms BEV (before Segmentation ~line 1279):**
   ```
   While domain choice is the primary factor, segmentation quality also plays a role, examined next.
   ```

3. **End of Segmentation as Necessary (before Template Planning ~line 1284):**
   ```
   Beyond segmentation and domain choice, the planning algorithm itself affects path stability, as discussed next.
   ```

4. **End of Template Planning and Turn Safety (before Failure Analysis ~line 1293):**
   ```
   The conditions under which this approach degrades are characterized in the following section.
   ```

5. **End of Failure Analysis (before Broader Implications ~line 1311):**
   ```
   Despite these boundary conditions, the geometric coverage insight generalizes beyond sidewalk navigation, as discussed next.
   ```

6. **End of Broader Implications (before Limitations ~line 1321):**
   ```
   Several limitations constrain the scope of these findings.
   ```

7. **End of Limitations (before Threats to Validity ~line 1338):**
   ```
   In addition to these limitations, specific threats to validity merit consideration.
   ```
</action>

<acceptance_criteria>
- [ ] All 7 section boundaries within Ch.5 have a one-sentence transition
- [ ] Transitions maintain the formal academic tone established in Phase 4
- [ ] No transition adds interpretive claims beyond what the sections already contain
</acceptance_criteria>

### Task 6: Verify front matter completeness (FRNT-01)

<read_first>
- `thesis/main.tex` lines 1-195 (entire front matter through Abstract)
</read_first>

<action>
Verify all front matter elements are present and correctly formatted. Check each item:

1. **Title page** (lines 95-114): Verify title, author, degree, year present
2. **Approval page** (lines 116-137): Verify advisor + 2 committee members listed. Confirm `[Committee Member Name]` placeholder remains per D-02
3. **Copyright page** (lines 139-147): Verify author name and year
4. **Acknowledgments** (lines 149-161): Verify present and appropriately personal. Confirm `[Committee Member]` placeholder in Acknowledgments also remains per D-02
5. **Table of Contents** (lines 163-165): Verify `\tableofcontents` command present
6. **List of Tables** (lines 167-170): Verify `\listoftables` present with TOC entry
7. **List of Figures** (lines 172-175): Verify `\listoffigures` present with TOC entry
8. **Abstract** (lines 177-189): Verify present and non-empty

Report any missing or malformed elements. Do NOT modify the committee member placeholder.
</action>

<acceptance_criteria>
- [ ] All 8 front matter elements confirmed present
- [ ] Committee member placeholder `[Committee Member Name]` remains on approval page (line 134)
- [ ] Committee member placeholder `[Committee Member]` remains in Acknowledgments (line 155)
- [ ] No front matter elements are missing or malformed
</acceptance_criteria>

### Task 7: Check line count and page estimate (FRNT-02)

<read_first>
- `thesis/main.tex` — run `wc -l thesis/main.tex` to get exact line count
- Review CONTEXT.md estimate: ~1393 lines = ~65-75 pages at OU double-spaced format
</read_first>

<action>
1. Count the current line total of `thesis/main.tex` (after adding transitions in Tasks 1-5)
2. Estimate page count using the OU double-spaced heuristic:
   - OU format: 12pt Times New Roman, double-spaced, 1.5" left margin, 1" other margins
   - Heuristic: ~20-22 lines of body text per page (double-spaced with margins)
   - But LaTeX lines != printed lines (commands, blank lines, environments reduce ratio)
   - Approximate: 1393 original + ~35 transition sentences = ~1428 lines
   - At roughly 18-20 effective text lines per page: ~71-79 pages
   - Target: 60-80 pages -- this should be well within range
3. If the estimate falls within 60-80 pages: report the count and confirm no action needed
4. If outside range: suggest specific minor adjustments (per D-03, prefer tightening verbose sections or expanding thin ones, no structural changes)

Report the final line count and page estimate as a comment at the end of the file or in the plan summary.
</action>

<acceptance_criteria>
- [ ] Final line count reported after all transition additions
- [ ] Page estimate computed and documented
- [ ] If within 60-80 pages: explicitly confirmed as meeting target
- [ ] If outside range: specific adjustment suggestions provided (no structural changes per D-03)
</acceptance_criteria>

## Execution Notes

- All transitions should be exactly one sentence, formal academic tone, third-person impersonal
- Use `Chapter~\ref{ch:...}` or `Section~\ref{sec:...}` cross-references where labels exist; otherwise use relative references ("the next section", "the following section")
- Do not add transitions at the very end of Ch.6 (Conclusion) -- the thesis ends there
- Do not modify any existing content -- only add transition sentences
- Tasks 1-5 modify `thesis/main.tex`; Tasks 6-7 are verification-only (no file changes unless issues found)

## Verification

After all tasks complete:
1. Grep for "next section\|next chapter\|following section\|reviewed next\|described next\|examined next" to confirm transitions were added
2. Verify no `\section` or `\chapter` boundaries were accidentally broken
3. Confirm final line count
