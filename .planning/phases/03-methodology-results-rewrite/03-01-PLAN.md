---
phase: 03-methodology-results-rewrite
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [thesis/main.tex]
autonomous: true
requirements: [NARR-03]

must_haves:
  truths:
    - "Every major System Design section starts with 1-2 WHY paragraphs before the WHAT description"
    - "Design rationale covers all 6 required components: SegFormer-B0, BEV homography, skeleton graph, template arc, image-space midpoint, teacher-student"
    - "Chapter opening sets up the design iteration narrative (v1 through v4)"
    - "BEV is presented with nuance: retained for corridor extraction and turns, not dismissed"
    - "All existing equations, figures, tables, and labels are preserved"
    - "Tone is measured thesis-formal, not assertive conference-paper style"
  artifacts:
    - path: "thesis/main.tex"
      provides: "Rewritten Chapter 3 System Design with design rationale"
      contains: "\\chapter{System Design}"
  key_links:
    - from: "thesis/main.tex Chapter 3"
      to: "thesis/main.tex Chapter 4 Evaluation"
      via: "Forward references to evaluation sections"
      pattern: "\\\\ref\\{sec:bev_fragility_results\\}"
    - from: "thesis/main.tex Chapter 3"
      to: "thesis/main.tex Chapter 3 internal"
      via: "Section labels preserved for cross-chapter references"
      pattern: "\\\\label\\{sec:segmentation\\}"
---

<objective>
Rewrite Chapter 3 (System Design) of thesis/main.tex with design rationale ("why before what") per NARR-03.

Purpose: The current System Design chapter describes pipeline components in a "what-it-does" style without explaining WHY each approach was chosen. Per D-01, each section must start with 1-2 paragraphs of rationale (constraint, trade-off, lesson from prior iteration) BEFORE the technical description. Per D-02, rationale must cover 6 specific components. The chapter opening must set up the design iteration narrative.

Output: Rewritten Chapter 3 (currently lines ~390-735 of thesis/main.tex) with WHY-then-WHAT structure throughout, growing by approximately 30-50 lines.
</objective>

<execution_context>
@C:/Users/miji0000/Desktop/thesis_prep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miji0000/Desktop/thesis_prep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-methodology-results-rewrite/03-CONTEXT.md
@.planning/phases/03-methodology-results-rewrite/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite chapter opening, Hardware Platform, Segmentation sections, and Resolution Trade-Off (lines ~390-505)</name>
  <files>thesis/main.tex</files>
  <read_first>
    - thesis/main.tex (lines 390-505 for current chapter opening through Training Progression; also lines 312-315 for Introduction reference to sec:bev_fragility_results; lines 1067-1086 for checkpoint benchmark that will be removed in Plan 02 so we understand the teacher-student narrative here; lines 706-735 for Evaluation Metrics and Software Architecture at end of chapter)
    - .planning/phases/03-methodology-results-rewrite/03-CONTEXT.md (D-01 why-then-what pattern, D-02 rationale components list, D-10 formal tone, D-11 BEV nuance)
    - .planning/phases/03-methodology-results-rewrite/03-RESEARCH.md (Architecture Patterns section for target structure; Design Rationale Source Material table; Design Rationale Writing Pattern code example; Pitfall 1 about WHY vs WHAT; Pitfall 6 about preserving technical content)
  </read_first>
  <action>
Rewrite the following sections in thesis/main.tex, preserving all equations, figures, tables, \label{} identifiers, and numerical values.

**1. Chapter opening paragraph (line ~395):**
Replace the current opening paragraph with a design philosophy overview. The new opening must:
- Frame the chapter as describing a modular pipeline evolved through four design iterations (v1: skeleton-graph in BEV, v2: DT ridge in BEV, v3: image-space midpoint bypass, v4: template-approval arcs)
- State the core design principle: each iteration was driven by a specific failure mode or constraint discovered in the previous one
- Mention the two-domain story: BEV for metric-scale reasoning (corridor extraction, turns) and image-space for efficient straight-ahead following
- Use measured thesis tone per D-10: "This chapter describes..." not "We present..."
- Keep the existing reference to Figure~\ref{fig:pipeline_diagram} and the Sections~\ref{sec:hardware}--\ref{sec:temporal_smoothing} navigation guide
- Preserve the \begin{figure} block for fig:pipeline_diagram exactly as-is

**2. Hardware Platform section (line ~406-409):**
Add a brief WHY paragraph (2-3 sentences) BEFORE the existing content. The rationale:
- The hardware constraint is the root constraint that cascades to all other design choices
- A single monocular camera under 15W limits the model size, inference strategy, and sensing modality
- No depth sensor, stereo, LiDAR, or IMU means all 3D reasoning must come from calibrated geometry (homography) rather than direct measurement
Then preserve existing WHAT content (the scooter description paragraph) with minor tone edits only.

**3. Segmentation Module section (lines ~413-500):**
Add 2 WHY paragraphs BEFORE the existing content at line 416 ("The first stage performs..."). These are the heaviest rationale paragraphs in the chapter. Follow the code example from 03-RESEARCH.md "Design Rationale Example: Segmentation Module":

WHY paragraph 1 -- SegFormer-B0 constraint (per D-02):
- The real-time constraint (10 Hz on RPi 4) limits parameter budget
- SegFormer-B0 (3.7M params) achieves sub-50ms inference on CPU, leaving ~50ms for downstream processing
- Larger variants (B2 at 24M, B5 at 84.7M) consume the entire frame budget on segmentation alone
- "The choice of SegFormer-B0 is therefore not a preference but a constraint"

WHY paragraph 2 -- Teacher-student and OneFormer choice (per D-02):
- Small models on limited data produce noisy masks
- Initial iteration used SegFormer-B2 teacher: student achieved IoU 0.758 (from tab:seg_comparison)
- Switching to OneFormer Swin-L teacher raised student IoU to 0.946 while reducing inference from 18.9ms to 11.7ms
- This motivates the teacher-student framework described in following subsections

Then preserve existing WHAT content from line 416 onward. The existing Teacher-Student subsection (lines 421-465), Hybrid Training Dataset, Loss Function, and Training Progression subsections should be preserved with only minor tone adjustments (remove any "we" statements, use passive or "this work" constructions per D-10).

**4. Resolution Trade-Off section (lines ~505-512):**
Add a brief WHY paragraph (2-3 sentences) explaining why resolution matters for the real-time constraint. The existing content describes the analysis; the rationale explains why this analysis was necessary: resolution directly trades segmentation quality against inference latency, and the chosen 640x360 is the sweet spot identified through systematic evaluation.

**CRITICAL PRESERVATION RULES:**
- All \label{} identifiers must remain: sec:hardware, sec:segmentation, sec:teacher_student, and any others in this range
- All equations (the confidence threshold equation around line 432, the loss function equation) must remain verbatim
- The \input{tables/generated/training_progression.tex} inclusion must remain
- All \cite{} references must remain
- Do NOT modify anything below the Resolution Trade-Off section (line ~512+) -- that is Task 2's territory
- Do NOT modify anything above the chapter opening (line 390) -- that belongs to Chapter 2
  </action>
  <verify>
    <automated>grep -n "constraint\|trade-off\|prior iteration\|abandoned\|lesson\|driven by\|forced\|limits\|alternative" thesis/main.tex | grep -A0 "39[0-9]\|4[0-9][0-9]\|50[0-5]" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for "constraint" or "trade-off" or "driven by" in lines 390-505 returns at least 4 matches (WHY paragraphs present)
    - grep for "four design iterations" or "four iterations" in the chapter opening returns at least 1 match
    - grep for "SegFormer-B0" AND "3.7M" in the Segmentation WHY paragraph returns at least 1 match
    - grep for "0.758" AND "0.946" in the teacher-student WHY paragraph returns at least 1 match
    - \label{sec:segmentation} and \label{sec:teacher_student} and \label{sec:hardware} all preserved
    - The confidence threshold equation (\tilde{y}(u)) is unchanged
    - \input{tables/generated/training_progression.tex} is present
    - grep for "We adopt" or "We use" or "We employ" returns 0 matches in the modified range (thesis tone per D-10)
    - No content below the Resolution Trade-Off section is modified
  </acceptance_criteria>
  <done>Chapter opening frames 4-iteration design narrative; Hardware, Segmentation, and Resolution sections all have WHY-before-WHAT structure; all equations/labels/figures preserved; thesis formal tone throughout</done>
</task>

<task type="auto">
  <name>Task 2: Add design rationale to BEV, Path Planning, and remaining sections (lines ~506-735)</name>
  <files>thesis/main.tex</files>
  <read_first>
    - thesis/main.tex (lines 506-735 for BEV Projection through Software Architecture; re-read lines 390-510 to see Task 1 changes and ensure consistency; lines 860-880 for tab:planner_comparison data needed for planner rationale; lines 1113-1135 for tab:template_eval data)
    - .planning/phases/03-methodology-results-rewrite/03-CONTEXT.md (D-02 required rationale components: BEV homography, skeleton graph, template arc, image-space midpoint; D-11 BEV nuance)
    - .planning/phases/03-methodology-results-rewrite/03-RESEARCH.md (Design Rationale Source Material table; Pitfall 1 WHY vs WHAT; Pitfall 3 BEV nuance; Pitfall 6 preserving technical content)
    - simulation_camera_scooter/RUNTIME_RUNBOOK.md (accepted run data for template planner rationale)
  </read_first>
  <action>
Add WHY rationale paragraphs to each major section from BEV Projection through the end of Chapter 3, preserving all existing technical content, equations, figures, table references, and labels.

**1. Bird's-Eye View Projection section (lines ~506-520):**
Add 1 WHY paragraph BEFORE existing content. Rationale per D-02 and D-11:
- BEV homography provides metric-scale coordinates needed for corridor extraction and turn planning
- Why not learned BEV (LSS, BEVFormer)? Those methods assume multi-camera rigs and GPU; a 4-point homography costs 0.9ms on CPU
- Acknowledge the flat-plane assumption and fixed-camera-pose limitation (tested empirically in Section~\ref{sec:bev_fragility_results})
- Per D-11: BEV is NOT dismissed -- it is essential for corridor verification and turns; the thesis argument is about choosing the right domain for each task

**2. BEV Mask Refinement section (lines ~523-540):**
Add 1 brief WHY paragraph. Rationale:
- Perspective warp amplifies segmentation noise at far distances
- Morphological cleanup is necessary to produce usable corridors for path planning
- Without refinement, small segmentation errors become large BEV artifacts

**3. Path Planning Methods section overview (lines ~543-550):**
Add 1-2 WHY paragraphs that frame the design iteration narrative for the planner subsections:
- The evolution from "discovery" (constructing paths from noisy BEV pixels) to "verification" (scoring pre-computed templates against a clean corridor)
- Four iterations explored different planning strategies; the progression reveals that the planning DOMAIN matters as much as the planning ALGORITHM
- Brief forward reference: the evaluation in Chapter~\ref{ch:evaluation} systematically compares all five methods

**4. BEV Skeleton-Graph Planner subsection (within Path Planning):**
Add WHY rationale per D-02: This was the v1 approach inspired by medial-axis road-following literature. Explain WHY it was tried (intuitive, well-studied) and WHY it was ultimately abandoned: Guo-Hall thinning on noisy BEV masks produces unstable skeletons; per-frame graph construction costs ~380ms; small segmentation errors cause catastrophic skeleton topology changes.

**5. BEV Distance-Transform Ridge Planner subsection:**
Add WHY rationale: Replaced skeleton extraction with EDT ridge following to avoid graph construction. Still operates in BEV domain. The ridge approach is more robust to mask noise but the BEV domain itself imposes a 926.8ms planner latency (from tab:planner_comparison).

**6. BEV Template Arc Planner subsection:**
Add WHY rationale per D-02: Shifts from "discover a path" to "verify pre-computed candidates." Instead of constructing paths from noisy pixels, propose a small bank of arcs and score each against the DT corridor. This eliminates noise-sensitive graph construction entirely. Results: 40.6% heading error reduction vs skeleton, 50% fewer path switches (from tab:template_eval). This is the v4 planner used in the accepted run.

**7. Image-Space Midpoint Planner subsection:**
Add WHY rationale per D-02: The v3 insight -- for straight-ahead corridor following, perspective projection preserves the lateral ordering of mask boundaries. The midpoint of each row in image-space approximates the corridor center without any BEV transform. Result: 14.3px center error at 2.2ms, a 421x speedup over BEV DT (from tab:planner_comparison). This is approximately what a naive baseline (raw mask center-following) achieves, suggesting the mask itself encodes sufficient geometric information for straight-ahead path extraction.

**8. Image-Space DT Planner subsection:**
Add brief WHY rationale: Provides a more robust fallback than midpoint when mask boundaries are irregular. 99.4% inside-GT at 108.1ms.

**9. GPS-Conditioned Waypoint-Turn Planner and Turn Containment Guard subsections:**
Add WHY rationale: BEV-domain reasoning IS needed for turn geometry (metric-scale arc fitting). The containment guard addresses the key safety concern: ensuring the committed path stays within the corridor before execution. Validated at 0% containment failure rate.

**10. Remaining sections (Temporal Smoothing through Software Architecture, lines ~640-735):**
Add brief 1-2 sentence WHY rationale where currently missing:
- Temporal Smoothing: frame-by-frame inference produces unavoidable jitter; EMA smoothing with reset hysteresis balances responsiveness with stability
- Object Detection: safety-critical requirement; YOLOv8n chosen for 3.2MB footprint and <30ms inference
- GPS Navigation: provides intent conditioning for turn planning; not used for localization
- Steering and Speed: pure pursuit chosen for simplicity and interpretability
- Safety Mechanisms: non-negotiable gates; design rationale is that any single failure mode must trigger a safe degradation
- Evaluation Metrics and Software Architecture: these sections are descriptive, not design-choice sections; preserve as-is with minor tone edits only

**CRITICAL PRESERVATION RULES:**
- All \label{} identifiers must remain exactly as-is
- All equations must remain verbatim
- All figure references and figure blocks must remain
- All table references must remain
- All \cite{} references must remain
- The reference to Section~\ref{sec:bev_fragility_results} on line 515 must remain
- Do NOT modify content above line 506 (Task 1's territory)
- Do NOT modify content below line 735 (Chapter 4 territory, Plan 02)

**TONE (per D-10):** Replace any "we" constructions with passive voice or "this work" / "the system" / "the pipeline". Use hedged language: "suggests," "indicates," "appears to." No assertive "demonstrates" or "proves."
  </action>
  <verify>
    <automated>grep -c "abandoned\|fragile\|constraint\|trade-off\|iteration\|domain\|rationale\|driven by\|motivated\|chosen\|noise-sensitive\|prior iteration" thesis/main.tex</automated>
  </verify>
  <acceptance_criteria>
    - grep for "discovery.*verification\|verification.*discovery\|propose-and-verify\|propose.*verify" in lines 540-610 returns at least 1 match (iteration narrative present)
    - grep for "homography\|0.9.*ms\|multi-camera\|flat.*plane" in BEV Projection WHY paragraph returns at least 2 matches
    - grep for "abandoned\|noise-sensitive\|unstable skeleton\|380.*ms" in Skeleton-Graph subsection returns at least 2 matches
    - grep for "421\|14.3.*px\|image-space.*straight" in Midpoint subsection returns at least 1 match
    - grep for "containment.*0%\|0%.*containment" in Turn Planner subsection returns at least 1 match
    - \label{sec:bev} and \label{sec:template_planner} and \label{sec:software_arch} and \label{sec:metrics} all preserved
    - grep for "We propose" or "We develop" or "We introduce" returns 0 matches in lines 506-735
    - All existing subsection labels under Path Planning Methods are preserved
    - Chapter 4 content (line 738+) is untouched
  </acceptance_criteria>
  <done>All System Design sections from BEV through Software Architecture have WHY-before-WHAT rationale; planner subsections tell the discovery-to-verification iteration story; BEV nuance preserved (retained for corridors/turns); all labels/equations/figures intact; formal thesis tone</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. grep -c "\\chapter{System Design}" thesis/main.tex returns 1
2. grep -c "\\label{ch:system_design}" thesis/main.tex returns 1
3. grep for "four.*iteration\|four design iteration\|v1.*v4\|design iteration" in Chapter 3 returns at least 2 matches
4. grep for "constraint\|trade-off\|driven by\|abandoned\|prior iteration\|noise-sensitive\|motivated" in lines 390-735 returns at least 15 matches (WHY rationale distributed across sections)
5. All required labels preserved: sec:hardware, sec:segmentation, sec:teacher_student, sec:bev, sec:template_planner, sec:software_arch, sec:metrics, sec:safety, fig:pipeline_diagram
6. No "We propose" / "We develop" / "We demonstrate" / "We show" in Chapter 3
7. Chapter 4 content at line 738+ is completely untouched
8. The BEV nuance is present: "corridor" appears at least 3 times in BEV-related sections; no absolute "BEV is bad" statements
</verification>

<success_criteria>
- Every System Design section has WHY-before-WHAT structure per D-01
- All 6 components from D-02 have explicit design rationale: SegFormer-B0, BEV homography, skeleton graph, template arc, image-space midpoint, teacher-student
- Chapter opening establishes the 4-iteration design narrative
- BEV is presented with nuance per D-11: essential for corridors/turns, not dismissed
- All equations, figures, tables, labels, and cross-references preserved
- Measured thesis tone throughout per D-10
- Chapter 4 (Experimental Evaluation) is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/03-methodology-results-rewrite/03-01-SUMMARY.md`
</output>
