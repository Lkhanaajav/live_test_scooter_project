---
phase: 03-methodology-results-rewrite
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: [thesis/main.tex]
autonomous: true
requirements: [NARR-04, EVAL-01, EVAL-02, EVAL-03, EVAL-04, STRUCT-02]

must_haves:
  truths:
    - "Chapter 4 is organized around 5 claim-driven sections following claim-evidence-conclusion pattern"
    - "Checkpoint benchmark table (tab:checkpoint_benchmark) is completely removed with no ghost references"
    - "Teacher-student progression narrative replaces the checkpoint benchmark"
    - "Design iteration progression table (v1-v4) is present with metrics"
    - "Runtime tables are merged into one clean table"
    - "Naive baseline discussion is present as conceptual lower bound"
    - "BEV nuance preserved: BEV retained for corridor extraction and turns"
    - "All kept tables and figures are preserved and correctly placed within claim sections"
  artifacts:
    - path: "thesis/main.tex"
      provides: "Restructured Chapter 4 Experimental Evaluation with claim-driven sections"
      contains: "\\chapter{Experimental Evaluation}"
  key_links:
    - from: "thesis/main.tex Chapter 4 Claim sections"
      to: "thesis/main.tex Chapter 3 System Design"
      via: "Back-references to system design sections"
      pattern: "\\\\ref\\{sec:template_planner\\}"
    - from: "thesis/main.tex Chapter 4"
      to: "thesis/main.tex Chapter 5 Discussion"
      via: "Discussion references evaluation tables"
      pattern: "\\\\ref\\{tab:oracle_comparison\\}"
    - from: "thesis/main.tex Chapter 4 BEV fragility"
      to: "thesis/main.tex Chapter 3 BEV section"
      via: "Preserved label for cross-chapter reference"
      pattern: "\\\\label\\{sec:bev_fragility_results\\}"
---

<objective>
Restructure Chapter 4 (Experimental Evaluation) of thesis/main.tex from flat results listing to 5 claim-driven sections per D-05, implementing NARR-04, STRUCT-02, EVAL-01, EVAL-02, EVAL-03, and EVAL-04.

Purpose: The current Evaluation chapter presents results as a flat list of experiments without a coherent claim-driven narrative. It must be reorganized around five claims (per D-05), each following claim-evidence-conclusion pattern (per D-06). The scientifically worthless checkpoint benchmark must be replaced with a teacher-student narrative (EVAL-01). Runtime tables must be merged (EVAL-03). An iteration progression table (EVAL-02) and naive baseline discussion (EVAL-04) must be added.

Output: Restructured Chapter 4 (currently lines ~738-1211 of thesis/main.tex) reorganized into claim-driven sections with approximately +20-30 lines net growth.
</objective>

<execution_context>
@C:/Users/miji0000/Desktop/thesis_prep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miji0000/Desktop/thesis_prep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-methodology-results-rewrite/03-CONTEXT.md
@.planning/phases/03-methodology-results-rewrite/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure chapter opening, Experimental Setup, Claim 1 (Segmentation), and Claim 2 (Planning Domain)</name>
  <files>thesis/main.tex</files>
  <read_first>
    - thesis/main.tex (lines 738-920 for current chapter opening through Oracle-Mask Experiment; lines 921-970 for BEV Skeleton visualization and BEV Fragility sections; lines 1055-1090 for Checkpoint Benchmark section to understand what is being removed; lines 1090-1110 for Qualitative Results; lines 1212-1260 for Discussion chapter that references tab:oracle_comparison, tab:planner_comparison)
    - .planning/phases/03-methodology-results-rewrite/03-CONTEXT.md (D-03 remove checkpoint, D-04 teacher-student narrative, D-05 five claims, D-06 claim-evidence-conclusion, D-07 iteration progression table, D-09 naive baseline, D-10 formal tone, D-11 BEV nuance)
    - .planning/phases/03-methodology-results-rewrite/03-RESEARCH.md (Target structure for Chapter 4; Claim-Evidence-Conclusion Pattern code example; Naive Baseline Discussion Pattern; NEW iteration progression table LaTeX; Table Inventory and Disposition; Pitfall 2 renamed-not-restructured; Pitfall 3 BEV nuance; Pitfall 4 iteration table numbers; Pitfall 5 ghost references; Label Preservation Checklist)
    - simulation_camera_scooter/RUNTIME_RUNBOOK.md (accepted run FPS for iteration table)
  </read_first>
  <action>
Rewrite Chapter 4 from line 738 through the end of the current Planner Comparison Study (approximately line 920). This task creates the new chapter opening, preserves Experimental Setup, adds the Naive Baseline subsection, creates Claim 1 (Segmentation Quality), and creates Claim 2 (Planning Domain). The current flat structure must be replaced with claim-driven sections.

**1. Chapter opening paragraph (line ~742):**
Replace the current opening ("This chapter presents experimental results organized by pipeline stage...") with a new opening that:
- States that this chapter evaluates the system through five specific claims
- Lists the 5 claims briefly (1 sentence each): (1) teacher-student improves segmentation, (2) image-space dominates BEV for straight-ahead, (3) BEV is fragile as primary planning domain, (4) template-approval is more stable than skeleton discovery, (5) complete pipeline sustains stable real-time operation
- Notes the experimental setup described first, then each claim examined in turn
- Use measured thesis tone: "This chapter examines five claims..." not "We demonstrate..."

**2. Experimental Setup section (lines ~745-795):**
KEEP the existing Experimental Setup section (Data Collection, Hand-Annotated Ground Truth, Evaluation Protocol subsections) with only minor tone edits (replace "we" constructions).

ADD a new subsection after Evaluation Protocol:

\subsection{Naive Baseline}
\label{sec:naive_baseline}

Add 1-2 paragraphs establishing the conceptual naive baseline per D-09. Use the pattern from 03-RESEARCH.md "Naive Baseline Discussion Pattern":
- A system that extracts the traversable region via segmentation but performs no geometric path planning
- For each row of the mask, select the midpoint between left and right boundaries
- Follow this row-by-row center without temporal smoothing, curvature estimation, or forward look-ahead
- This is approximately what the image-space midpoint planner implements, minus connected-component filtering and Savitzky-Golay smoothing
- The remarkable performance of image-space midpoint (14.3 px center error at 2.2 ms) suggests the mask itself encodes sufficient geometric information for straight-ahead corridor following
- Frame this as a conceptual lower bound, NOT a new experiment

**3. Claim 1: Teacher-Student Training Improves Segmentation Quality (REPLACES current Segmentation Results section):**

Create a new section:
\section{Teacher--Student Training Improves Segmentation Quality}
\label{sec:claim_seg_quality}

Follow the claim-evidence-conclusion pattern per D-06:

STATE claim (1 paragraph):
- "This section examines the claim that a high-capacity teacher model substantially improves the quality of a lightweight student segmentation network for sidewalk navigation."
- If supported, this justifies the teacher-student framework described in Section~\ref{sec:teacher_student}

PRESENT evidence:
- Move the existing content from current "Segmentation Results" section (lines ~796-855)
- Include tab:seg_comparison, fig:segmentation_stage, fig:seg_improvement, tab:fullvideo_replay
- ADD teacher-student progression narrative per D-04 (REPLACES checkpoint benchmark):
  - 2-3 paragraphs describing: SegFormer-B2 teacher -> IoU 0.758; switch to OneFormer Swin-L teacher -> IoU 0.946; inference time also improved from 18.9ms to 11.7ms due to better model convergence
  - Reference the existing tab:seg_comparison for quantitative comparison
  - Note that the tab:training_progression table in Chapter 3 documents the full training history
  - This narrative REPLACES the checkpoint benchmark section entirely
- Move fig:seg_comparison_qual (currently in separate Qualitative Results section around line 1090-1110) into this claim section as qualitative evidence

CONCLUDE (1 paragraph):
- The evidence supports the claim that teacher quality is the dominant factor in student mask quality
- The switch from B2 to OneFormer teacher produced a 25% IoU improvement
- Qualification: evaluation is on campus sidewalk sequences; generalization to other environments is not tested

**4. Claim 2: Image-Space Planning Dominates BEV for Straight-Ahead Following (REPLACES current Planner Comparison Study):**

Create a new section:
\section{Image-Space Planning Dominates BEV for Straight-Ahead Following}
\label{sec:claim_planning_domain}

Follow the claim-evidence-conclusion pattern per D-06. Use the code example from 03-RESEARCH.md "Claim-Evidence-Conclusion Example: Claim 2":

STATE claim (1 paragraph):
- "This section examines whether image-space path planning offers advantages over BEV-domain planning for straight-ahead sidewalk following."
- If supported, BEV projection overhead can be avoided for the most common navigation scenario
- Per D-11: Scope explicitly to "straight-ahead following" -- BEV reserved for situations requiring metric-scale reasoning

PRESENT evidence:
- Move existing Planner Comparison content (tab:planner_comparison, fig:planner_comparison) into this section
- Move Oracle-Mask Experiment content (tab:oracle_comparison) into this section
- Reference the naive baseline from Section~\ref{sec:naive_baseline}
- ADD iteration progression table per D-07 (EVAL-02). Use the LaTeX from 03-RESEARCH.md:

\begin{table}[h]
\centering
\caption{Design iteration progression. Each iteration introduces a new planning strategy while remaining backward-compatible with earlier stages.}
\label{tab:iteration_progression}
\begin{tabular}{clllr}
\toprule
Iter. & Planner & Domain & Key Change & Pipeline FPS \\
\midrule
v1 & Skeleton-Graph & BEV & Guo--Hall thinning + Dijkstra & 9.1 \\
v2 & DT Ridge & BEV & EDT ridge replaces skeleton & ${\sim}$1\textsuperscript{*} \\
v3 & Midpoint / DT & Image-space & BEV bypass for straight-ahead & 59.2 \\
v4 & Template Arc & BEV corridor & Propose-and-verify arcs & 9.97 \\
\bottomrule
\end{tabular}
\begin{flushleft}
\footnotesize\textsuperscript{*}Estimated from component timings (926.8\,ms planner-only); measurements are not directly comparable across iterations due to different pipeline configurations.
\end{flushleft}
\end{table}

- Move fig:planner_comparison_qual and fig:skeleton_stage here if they provide relevant visual evidence for the planning domain comparison
- Connect the existing "Key Findings" items to the claim (rewrite as flowing paragraphs, not a flat list)

CONCLUDE (1 paragraph):
- Evidence supports the claim that image-space planning offers substantial advantages for straight-ahead following
- Image-space midpoint: ~4.5x lower lateral error at ~421x lower latency
- This advantage persists even with oracle masks (tab:oracle_comparison)
- Per D-11: "These results do not imply that BEV processing should be abandoned" -- BEV remains valuable for corridor extraction and turn planning
- "Rather, they suggest that the default planning domain for straight-ahead following should be image-space"

**CRITICAL RULES:**
- Preserve ALL table and figure \begin{} blocks exactly (move them but do not alter their content)
- Preserve ALL \label{} identifiers -- tables, figures, and sections
- The checkpoint benchmark section (tab:checkpoint_benchmark, lines 1055-1090) must NOT appear in this task's output -- it is being removed per D-03. Do NOT reference it or include any residual mention
- After removing checkpoint content, search for and remove any references to it: grep for "checkpoint_benchmark" and "best checkpoint" in the entire modified range
- Do NOT modify content from Claim 3 onward (BEV Fragility section at line ~921+) -- that is Task 2's territory
- Use measured thesis tone throughout per D-10

**LINE MANAGEMENT:** This task replaces lines ~738-920 and eliminates lines ~1055-1090 (checkpoint benchmark) and ~1090-1110 (qualitative results, moved into claims). The net result should be approximately the same line count or slightly more for this portion.
  </action>
  <verify>
    <automated>grep -c "claim\|Claim\|evidence.*support\|evidence.*presented\|examines the claim\|conclude" thesis/main.tex</automated>
  </verify>
  <acceptance_criteria>
    - grep for "examines the claim\|examines.*claim" in Chapter 4 returns at least 2 matches (Claims 1 and 2)
    - grep for "claim_seg_quality" returns 1 match (Claim 1 label exists)
    - grep for "claim_planning_domain" returns 1 match (Claim 2 label exists)
    - grep for "tab:iteration_progression" returns at least 2 matches (label and at least one reference)
    - grep for "sec:naive_baseline" returns at least 2 matches (label and at least one reference)
    - grep for "checkpoint_benchmark" returns 0 matches in the entire file (removed per D-03, EVAL-01)
    - grep for "tab:seg_comparison" returns at least 1 match (kept in Claim 1)
    - grep for "tab:planner_comparison" returns at least 1 match (kept in Claim 2)
    - grep for "tab:oracle_comparison" returns at least 1 match (kept in Claim 2)
    - grep for "five claims\|5 claims" or individual claim references in chapter opening returns at least 1 match
    - grep for "We demonstrate\|We show that\|We prove" returns 0 matches in Chapter 4
    - grep for "straight-ahead" in Claim 2 returns at least 2 matches (scope limiter per D-11)
    - The tab:iteration_progression table contains "v1" through "v4" and "9.97" for v4 FPS
  </acceptance_criteria>
  <done>Chapter 4 opening introduces 5 claims; Experimental Setup preserved with naive baseline added; Claim 1 restructures segmentation results with teacher-student narrative replacing checkpoint benchmark; Claim 2 restructures planner comparison with iteration progression table; all kept tables/figures moved to correct claim sections; checkpoint benchmark completely removed</done>
</task>

<task type="auto">
  <name>Task 2: Create Claims 3-5, merge runtime tables, and verify all cross-references</name>
  <files>thesis/main.tex</files>
  <read_first>
    - thesis/main.tex (read the FULL chapter 4 after Task 1 changes -- from \chapter{Experimental Evaluation} through \chapter{Discussion}; also read lines 310-315 for Introduction ref to sec:bev_fragility_results; lines 515 for System Design ref to sec:bev_fragility_results; lines 1212-1260 for Discussion refs to tab:oracle_comparison, tab:planner_comparison, tab:template_eval)
    - .planning/phases/03-methodology-results-rewrite/03-CONTEXT.md (D-05 claims 3-5, D-06 claim-evidence-conclusion, D-08 merge runtime tables, D-11 BEV nuance)
    - .planning/phases/03-methodology-results-rewrite/03-RESEARCH.md (Target structure for Claims 3-5; Merged Runtime Table LaTeX; Table Inventory and Disposition; Label Preservation Checklist; Pitfall 3 BEV nuance; Pitfall 7 chapter length; Pitfall 8 cross-reference breakage)
  </read_first>
  <action>
Complete the Chapter 4 restructure by creating Claims 3-5, merging runtime tables, handling the Temporal Smoothing section, and performing a cross-reference verification pass.

**1. Claim 3: Monocular BEV Projection is Fragile as Primary Planning Domain (REPLACES current BEV Fragility Analysis + BEV Skeleton-Graph Visualization):**

Create a new section:
\section{Monocular BEV Projection is Fragile as Primary Planning Domain}
\label{sec:claim_bev_fragility}
\label{sec:bev_fragility_results}

IMPORTANT: Use BOTH labels. The sec:bev_fragility_results label is referenced from Chapter 1 (line 312), Chapter 3 (line 515), and possibly Discussion. It MUST be preserved.

Follow claim-evidence-conclusion pattern per D-06:

STATE claim:
- "This section examines the claim that monocular BEV projection, while useful for metric-scale reasoning, is unreliable as the primary planning domain for straight-ahead sidewalk following."
- If supported, this motivates the dual-domain architecture: BEV for corridor validation and turns, image-space for primary path following

PRESENT evidence:
- Move existing BEV Fragility content (tab:bev_fragility, fig:bev_fragility) here
- Move existing BEV Skeleton-Graph Pipeline Visualization content (fig:skeleton_stage) here if not already moved to Claim 2
- Key data: 99.3% frame failure rate in profiled sequence
- Connect to the oracle-mask experiment from Claim 2: even with perfect masks, BEV planning produces higher error

CONCLUDE:
- Evidence supports the claim that monocular BEV is fragile as primary planning domain
- Per D-11 NUANCE (CRITICAL): "This fragility does not render BEV processing without value. As Claim~5 demonstrates, the final pipeline retains BEV projection for corridor extraction and turn validation, where metric-scale reasoning is essential. The evidence suggests that the role of BEV should be verification of pre-computed candidates rather than primary path discovery."

**2. Claim 4: Template-Approval Planning is More Stable than Skeleton Discovery (REPLACES current Template Arc Planner Evaluation):**

Create a new section:
\section{Template-Approval Planning is More Stable than Skeleton Discovery}
\label{sec:claim_template_approval}

STATE claim:
- "This section examines the claim that a propose-and-verify template approach produces more stable paths than constructing paths from scratch via skeleton extraction."
- If supported, this validates the v4 design iteration's shift from discovery to verification

PRESENT evidence:
- Move existing Template Arc Planner Evaluation content (tab:template_eval) here
- Reference Section~\ref{sec:template_planner} for system design context
- Key data: 40.6% heading error reduction, 50% fewer path switches vs skeleton baseline
- Both planners tested on identical 220-frame clip with same segmentation and BEV config

CONCLUDE:
- Template-approval achieves substantially lower heading error and fewer path discontinuities
- The propose-and-verify paradigm eliminates noise-sensitive graph construction
- Qualification: comparison is on a single clip; broader validation is provided by Claim 5 (accepted run)

**3. Claim 5: Complete Pipeline Sustains Stable Real-Time Operation (REPLACES current Runtime Analysis + Waypoint-Turn Evaluation + Full-Length Accepted Run + Overnight Containment):**

Create a new section:
\section{Complete Pipeline Sustains Stable Real-Time Operation}
\label{sec:claim_system_validation}

STATE claim:
- "This section examines whether the complete pipeline---integrating the findings of Claims 1--4---sustains stable real-time operation across extended video sequences."
- If supported, this provides end-to-end validation of the design iteration narrative

PRESENT evidence:
This section consolidates 4 current sections into one coherent argument. Present in this order:

a) Runtime analysis with MERGED runtime table per D-08 (EVAL-03):
Replace tab:runtime_comparison and tab:runtime_offenders with a single merged table. Use the LaTeX from 03-RESEARCH.md:

\begin{table}[h]
\centering
\caption{Per-module runtime comparison at $640{\times}360$ (CPU-only), with the planner module showing the dominant speedup from image-space processing.}
\label{tab:runtime_merged}
\begin{tabular}{lccc}
\toprule
Module & BEV Pipeline & Image-Space & Note \\
\midrule
SegFormer Inference   & 11.7\,ms & 11.7\,ms & Shared \\
Mask Refinement       & 8.5\,ms  & 3.0\,ms  & Img skips BEV cleanup \\
BEV Projection        & 0.9\,ms  & ---      & \\
BEV Cleanup           & 14.6\,ms & ---      & \\
Planner               & 380.3\,ms & 2.2\,ms & $173\times$ speedup \\
\midrule
Total                 & 416.0\,ms & 16.9\,ms & \\
FPS                   & 2.4       & \textbf{59.2}  & \\
\bottomrule
\end{tabular}
\end{table}

Keep tab:runtime_configs as a separate supporting table (per D-08).

REMOVE the old tab:runtime_comparison and tab:runtime_offenders tables entirely -- they are replaced by tab:runtime_merged.

b) Waypoint-Turn Planner evaluation:
- Move existing content (tab:waypoint_turn_eval) here
- Key data: 0% containment failure rate, 12.9 FPS

c) Full-Length Accepted Run:
- Move existing content (tab:accepted_run) here
- Key data: 1800 frames, 100% template source, 9.97 FPS, IoU 0.954

d) Overnight Containment Validation:
- Move existing content here
- Near-ego exclusion parameter resolves BEV edge noise

CONCLUDE:
- The complete pipeline achieves 9.97 FPS on the accepted run (meeting the 10 Hz target within measurement noise)
- Template-approval planner is used for 100% of frames in the accepted run
- Turn planner maintains 0% containment failure with safety guard
- These results validate the design iteration narrative: from fragile BEV skeleton (v1) to stable dual-domain template-approval (v4)

**4. Temporal Smoothing Evaluation section:**
KEEP this section with minor edits for tone. It does not fit neatly into a claim but provides supporting evidence. Place it after Claim 5 as a brief standalone section. Remove any reference to "best checkpoint" that was connected to the now-removed checkpoint benchmark.

**5. Cross-Reference Verification Pass (CRITICAL):**
After all restructuring, verify these specific cross-references:
- \label{sec:bev_fragility_results} MUST exist (referenced from Introduction line 312 and System Design line 515)
- \label{ch:evaluation} MUST exist on the chapter
- tab:oracle_comparison MUST exist (referenced from Discussion)
- tab:planner_comparison MUST exist (referenced from Discussion)
- tab:template_eval MUST exist (referenced from Discussion)
- Search entire file for "checkpoint_benchmark" -- must return 0 matches
- Search entire file for "tab:runtime_comparison" -- must return 0 matches (replaced by tab:runtime_merged)
- Search entire file for "tab:runtime_offenders" -- must return 0 matches (merged into tab:runtime_merged)
- If any of the old labels (runtime_comparison, runtime_offenders) are referenced from OTHER chapters, update those references to tab:runtime_merged

**6. Ghost Reference Cleanup per Pitfall 5:**
- Remove any remaining references to the checkpoint benchmark from the Temporal Smoothing section or anywhere else
- Update any references to old section names that were renamed during restructuring
- If any section references point to removed content, update or remove them

**CRITICAL PRESERVATION RULES:**
- All kept tables (tab:video_dataset, tab:seg_comparison, tab:fullvideo_replay, tab:planner_comparison, tab:oracle_comparison, tab:bev_fragility, tab:runtime_configs, tab:template_eval, tab:waypoint_turn_eval, tab:accepted_run) must have their \begin{table}...\end{table} blocks preserved exactly
- All kept figures must have their \begin{figure}...\end{figure} blocks preserved exactly
- Chapter 3 (System Design) must NOT be modified by this task
- Chapter 5 (Discussion) must NOT be modified by this task
- Use measured thesis tone throughout per D-10

**LENGTH CHECK per Pitfall 7:** The restructured Chapter 4 should be approximately 470-520 lines of LaTeX. If it exceeds ~520 lines, tighten existing prose rather than removing new content.
  </action>
  <verify>
    <automated>grep -c "claim_bev_fragility\|claim_template_approval\|claim_system_validation\|bev_fragility_results\|naive_baseline\|tab:runtime_merged\|tab:iteration_progression" thesis/main.tex</automated>
  </verify>
  <acceptance_criteria>
    - grep for "claim_bev_fragility" returns at least 1 match (Claim 3 label exists)
    - grep for "bev_fragility_results" returns at least 1 match (preserved legacy label)
    - grep for "claim_template_approval" returns at least 1 match (Claim 4 label exists)
    - grep for "claim_system_validation" returns at least 1 match (Claim 5 label exists)
    - grep for "tab:runtime_merged" returns at least 2 matches (label + reference)
    - grep for "tab:runtime_comparison" returns 0 matches (old table removed)
    - grep for "tab:runtime_offenders" returns 0 matches (old table removed)
    - grep for "checkpoint_benchmark" returns 0 matches in entire file
    - grep for "tab:template_eval" returns at least 1 match (kept)
    - grep for "tab:waypoint_turn_eval" returns at least 1 match (kept)
    - grep for "tab:accepted_run" returns at least 1 match (kept)
    - grep for "99.3" in Claim 3 returns at least 1 match (BEV fragility data)
    - grep for "40.6\|heading error reduction" in Claim 4 returns at least 1 match (template eval data)
    - grep for "9.97\|1800" in Claim 5 returns at least 1 match (accepted run data)
    - The BEV nuance paragraph in Claim 3 conclusion contains "corridor" or "verification" or "metric-scale" (per D-11)
    - Chapter 3 (System Design) content is untouched
    - Chapter 5 (Discussion) content is untouched
    - No LaTeX undefined reference warnings would be produced (all labels referenced exist)
  </acceptance_criteria>
  <done>Claims 3-5 created with claim-evidence-conclusion structure; runtime tables merged into tab:runtime_merged; checkpoint benchmark fully removed with no ghost references; all cross-references verified; BEV nuance preserved; Temporal Smoothing section retained; Chapter 4 is complete claim-driven evaluation</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. grep -c "\\chapter{Experimental Evaluation}" thesis/main.tex returns 1
2. grep -c "\\label{ch:evaluation}" thesis/main.tex returns 1
3. Chapter 4 has exactly 5 claim sections plus Experimental Setup and Temporal Smoothing
4. grep for "examines the claim\|examines.*claim" returns at least 5 matches (one per claim)
5. grep for "evidence.*support\|evidence.*presented" returns at least 5 matches (conclusions present)
6. tab:iteration_progression table exists with v1-v4 data
7. tab:runtime_merged table exists replacing old runtime_comparison and runtime_offenders
8. Naive baseline subsection exists in Experimental Setup
9. grep for "checkpoint_benchmark" returns 0 matches in entire file
10. grep for "tab:runtime_comparison" returns 0 matches (replaced)
11. grep for "tab:runtime_offenders" returns 0 matches (merged)
12. All preserved labels verified: ch:evaluation, sec:bev_fragility_results, tab:seg_comparison, tab:planner_comparison, tab:oracle_comparison, tab:bev_fragility, tab:template_eval, tab:waypoint_turn_eval, tab:accepted_run, tab:runtime_configs
13. Chapter 3 (System Design) NOT modified
14. Chapter 5 (Discussion) NOT modified
15. No "We demonstrate" or "We show that" or "We prove" in Chapter 4
</verification>

<success_criteria>
- Chapter 4 reorganized around 5 claim-driven sections per D-05 (STRUCT-02)
- Each claim follows claim-evidence-conclusion pattern per D-06 (NARR-04)
- Checkpoint benchmark removed and replaced with teacher-student narrative (EVAL-01)
- Iteration progression table present with v1-v4 metrics (EVAL-02)
- Runtime tables merged into single clean table (EVAL-03)
- Naive baseline discussion present as conceptual lower bound (EVAL-04)
- BEV nuance preserved throughout per D-11: BEV retained for corridor extraction and turns
- All kept tables and figures preserved with correct placement
- All cross-references verified and no ghost references remain
- Measured thesis tone throughout per D-10
- Chapter 3 and Chapter 5 content untouched
</success_criteria>

<output>
After completion, create `.planning/phases/03-methodology-results-rewrite/03-02-SUMMARY.md`
</output>
