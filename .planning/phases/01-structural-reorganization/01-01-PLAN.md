---
phase: 1
plan: 01-01
title: "Merge Closed-Loop chapter into System Design and update chapter structure"
wave: 1
depends_on: []
files_modified: ["thesis/main.tex"]
requirements: ["STRUCT-01"]
autonomous: true
---

# Plan 01-01: Merge Closed-Loop Chapter into System Design

## Objective

Eliminate Chapter 4 (Closed-Loop Control and System Integration) as a standalone chapter by moving its five sections into Chapter 3 (System Design and Methodology). Update all chapter labels, chapter titles, and the Thesis Organization paragraph to reflect the new 6-chapter structure. After this plan, the thesis has exactly 6 chapters with consistent labels and cross-references.

## must_haves

- [ ] No `\chapter{Closed-Loop Control and System Integration}` exists in main.tex
- [ ] No `\label{ch:closed_loop}` exists in main.tex
- [ ] No `\ref{ch:closed_loop}` exists in main.tex
- [ ] Ch.3 contains sections: Lightweight Object Detection, GPS Waypoint Navigation, Steering and Speed Computation, Serial Command Protocol, Safety Mechanisms
- [ ] Chapter labels are: `ch:introduction`, `ch:background`, `ch:system_design`, `ch:evaluation`, `ch:discussion`, `ch:conclusion`
- [ ] Thesis Organization paragraph references exactly 5 chapters (2-6) with correct new labels
- [ ] LaTeX compiles without undefined reference warnings for chapter labels

---

## Task 1: Move Closed-Loop sections into Chapter 3

<read_first>
- `thesis/main.tex` lines 607-737 (Temporal Smoothing through end of Closed-Loop chapter)
</read_first>

<action>
**Step 1a:** Remove the Chapter 4 header block and intro paragraph. Find and delete these exact lines (L658-664):

```
% =====================================================================
\chapter{Closed-Loop Control and System Integration}
\label{ch:closed_loop}
% =====================================================================

This chapter extends the offline perception pipeline to a real-time system capable of autonomous sidewalk navigation. We describe four additions: obstacle detection, GPS waypoint navigation, steering and speed computation, and the serial command protocol.
```

Replace with nothing (delete entirely). The five sections that follow (L666-737: Lightweight Object Detection through Safety Mechanisms) will remain in place — they now fall inside Chapter 3.

**Step 1b:** Insert a transition comment before the moved sections. Find the line:

```
\section{Lightweight Object Detection}
```

And insert immediately before it:

```
% -----------------------------------------------------------------
% --- Sections below merged from former Closed-Loop chapter --------
% -----------------------------------------------------------------
```

**Step 1c:** Reorder: move the Evaluation Metrics section (L627-648) and Software Architecture section (L652-655) to AFTER the Safety Mechanisms section (which ends at L737). This ensures pipeline-order per D-05:

The final section order in Chapter 3 should be:
1. Hardware Platform
2. Segmentation Module and Supervision Strategy
3. Resolution Trade-Off Analysis
4. Bird's-Eye View Projection
5. BEV Mask Refinement
6. Path Planning Methods
7. Temporal Smoothing
8. Lightweight Object Detection (merged)
9. GPS Waypoint Navigation (merged)
10. Steering and Speed Computation (merged)
11. Serial Command Protocol (merged)
12. Safety Mechanisms (merged)
13. Evaluation Metrics (moved after merged content)
14. Software Architecture (moved after merged content)

To do this:
- Cut the Evaluation Metrics section (from `\section{Evaluation Metrics}` through the end of its content, about L627-648) and the Software Architecture section (from `\section{Software Architecture}` through its content, about L652-655)
- Paste them after the Safety Mechanisms section's closing content (after the `\end{itemize}` that ends the safety bullet list)
- Ensure a `% -----------------------------------------------------------------` separator comment appears before each moved section
</action>

<acceptance_criteria>
- `grep -c "\\\\chapter{Closed-Loop" thesis/main.tex` returns 0
- `grep -c "\\\\label{ch:closed_loop}" thesis/main.tex` returns 0
- `grep -c "\\\\section{Lightweight Object Detection}" thesis/main.tex` returns 1
- `grep -c "\\\\section{Safety Mechanisms}" thesis/main.tex` returns 1
- The section `\section{Lightweight Object Detection}` appears AFTER `\section{Temporal Smoothing}` and BEFORE `\section{Evaluation Metrics}`
- The section `\section{Software Architecture}` appears AFTER `\section{Safety Mechanisms}`
</acceptance_criteria>

---

## Task 2: Update Chapter 3 intro paragraph

<read_first>
- `thesis/main.tex` line 374 (Ch.3 intro paragraph)
</read_first>

<action>
Find the exact string:

```
This chapter describes the perception pipeline that transforms monocular RGB frames into controller-ready waypoints. The system avoids reliance on GPS or external localization for local path following, leveraging a modular pipeline of interpretable geometric and learning-based modules. Figure~\ref{fig:pipeline_diagram} illustrates the overall architecture.
```

Replace with:

```
This chapter describes the complete navigation system, from monocular RGB perception through path planning to closed-loop vehicle control. The system avoids reliance on GPS or external localization for local path following, leveraging a modular pipeline of interpretable geometric and learning-based modules. Sections~\ref{sec:hardware}--\ref{sec:temporal_smoothing} cover perception and planning; Sections~\ref{sec:object_detection}--\ref{sec:safety} cover obstacle detection, GPS integration, steering, and safety mechanisms. Figure~\ref{fig:pipeline_diagram} illustrates the overall architecture.
```
</action>

<acceptance_criteria>
- `grep "complete navigation system" thesis/main.tex` matches exactly one line
- `grep "Sections.*object_detection.*safety" thesis/main.tex` matches exactly one line
</acceptance_criteria>

---

## Task 3: Rename chapter labels and titles

<read_first>
- `thesis/main.tex` lines 287-289 (Ch.2 header), 370-372 (Ch.3 header), 740-742 (Ch.5 header)
</read_first>

<action>
**Step 3a — Chapter 2 title and label.** Find:

```
\chapter{Literature Review}
\label{ch:related_work}
```

Replace with:

```
\chapter{Background and Related Work}
\label{ch:background}
```

**Step 3b — Chapter 3 title and label.** Find:

```
\chapter{System Design and Methodology}
\label{ch:methodology}
```

Replace with:

```
\chapter{System Design}
\label{ch:system_design}
```

**Step 3c — Former Chapter 5 (now Chapter 4) title and label.** Find:

```
\chapter{Experiments and Results}
\label{ch:results}
```

Replace with:

```
\chapter{Experimental Evaluation}
\label{ch:evaluation}
```

Note: `ch:discussion` and `ch:conclusion` labels keep their current names (they just become Ch.5 and Ch.6 instead of Ch.6 and Ch.7 — LaTeX auto-numbers).
</action>

<acceptance_criteria>
- `grep "\\\\label{ch:background}" thesis/main.tex` matches exactly one line
- `grep "\\\\label{ch:system_design}" thesis/main.tex` matches exactly one line
- `grep "\\\\label{ch:evaluation}" thesis/main.tex` matches exactly one line
- `grep "\\\\label{ch:related_work}" thesis/main.tex` returns no matches
- `grep "\\\\label{ch:methodology}" thesis/main.tex` returns no matches
- `grep "\\\\label{ch:results}" thesis/main.tex` returns no matches
- `grep "Background and Related Work" thesis/main.tex` matches one line
- `grep "Experimental Evaluation" thesis/main.tex` matches one line
</acceptance_criteria>

---

## Task 4: Update all cross-references to renamed chapter labels

<read_first>
- `thesis/main.tex` line 263 (Thesis Organization paragraph — contains ALL `\ref{ch:*}` usages)
</read_first>

<action>
**Step 4a — Thesis Organization paragraph.** Find the entire line 263 (the long paragraph starting with "The remainder of this thesis"):

```
The remainder of this thesis is organized as follows. Chapter~\ref{ch:related_work} reviews related work in autonomous navigation, segmentation, BEV reconstruction, path planning, and embedded perception. Chapter~\ref{ch:methodology} describes the proposed perception pipeline in detail, including the segmentation module, BEV projection, five planning methods, and temporal smoothing. Chapter~\ref{ch:closed_loop} presents the closed-loop control extension, including object detection, GPS navigation, and the serial command protocol. Chapter~\ref{ch:results} reports experimental results including the planner comparison study, BEV fragility analysis, runtime profiling, template planner evaluation, waypoint-turn planner validation, and a 1800-frame accepted run. Chapter~\ref{ch:discussion} discusses the implications of key findings, limitations, and threats to validity. Chapter~\ref{ch:conclusion} concludes with a summary and directions for future work.
```

Replace with:

```
The remainder of this thesis is organized as follows. Chapter~\ref{ch:background} reviews related work in autonomous navigation, segmentation, BEV reconstruction, path planning, and embedded perception. Chapter~\ref{ch:system_design} describes the complete system design, including the segmentation module, BEV projection, five planning methods, temporal smoothing, obstacle detection, GPS navigation, steering computation, and safety mechanisms. Chapter~\ref{ch:evaluation} reports experimental results including the planner comparison study, BEV fragility analysis, runtime profiling, template planner evaluation, waypoint-turn planner validation, and an 1800-frame accepted run. Chapter~\ref{ch:discussion} discusses the implications of key findings, limitations, and threats to validity. Chapter~\ref{ch:conclusion} concludes with a summary and directions for future work.
```

**Step 4b — Verify no stale refs remain.** Search the entire file for any remaining `\ref{ch:related_work}`, `\ref{ch:methodology}`, `\ref{ch:results}`, or `\ref{ch:closed_loop}`. Per the research, line 263 is the only location, but verify and fix any others found.
</action>

<acceptance_criteria>
- `grep "\\\\ref{ch:closed_loop}" thesis/main.tex` returns no matches
- `grep "\\\\ref{ch:related_work}" thesis/main.tex` returns no matches
- `grep "\\\\ref{ch:methodology}" thesis/main.tex` returns no matches
- `grep "\\\\ref{ch:results}" thesis/main.tex` returns no matches
- `grep "\\\\ref{ch:background}" thesis/main.tex` matches at least one line
- `grep "\\\\ref{ch:system_design}" thesis/main.tex` matches at least one line
- `grep "\\\\ref{ch:evaluation}" thesis/main.tex` matches at least one line
</acceptance_criteria>

---

## Task 5: Update Chapter 4 (former Ch.5) intro paragraph

<read_first>
- `thesis/main.tex` line 744 (Experiments and Results intro paragraph)
</read_first>

<action>
Find:

```
This chapter presents results organized by pipeline stage, followed by the planner comparison study, BEV fragility analysis, and runtime profiling.
```

Replace with:

```
This chapter presents experimental results organized by pipeline stage, followed by the planner comparison study, BEV fragility analysis, runtime profiling, and system-level validation runs.
```

This is a minor update for consistency with the new chapter title ("Experimental Evaluation").
</action>

<acceptance_criteria>
- `grep "experimental results organized" thesis/main.tex` matches exactly one line
</acceptance_criteria>

---

## Verification

After all tasks, run these checks:
1. Count `\chapter{` occurrences — should be exactly 6 (excluding `\chapter*` for front matter)
2. Count `\label{ch:` occurrences — should be exactly 6
3. No `\ref{ch:closed_loop}`, `\ref{ch:related_work}`, `\ref{ch:methodology}`, or `\ref{ch:results}` anywhere
4. Sections Object Detection through Safety Mechanisms appear between Temporal Smoothing and Evaluation Metrics

---

*Plan: 01-01 | Phase: 01-structural-reorganization*
*Created: 2026-03-30*
