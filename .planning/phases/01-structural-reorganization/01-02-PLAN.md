---
phase: 1
plan: 01-02
title: "Fix iteration count to four and clean up remaining consistency issues"
wave: 2
depends_on: ["01-01"]
files_modified: ["thesis/main.tex"]
requirements: ["STRUCT-04"]
autonomous: true
---

# Plan 01-02: Fix Iteration Count and Consistency Cleanup

## Objective

Fix the four locations where the thesis incorrectly says "three design iterations" (should be "four"), update the Abstract and Conclusion iteration lists to include the fourth iteration, and verify all cross-references are consistent after the structural merge from Plan 01-01.

## must_haves

- [ ] No occurrence of "three design iterations" or "three iterations" remains in the thesis
- [ ] The Abstract iteration list names all four iterations
- [ ] The Conclusion Summary of Contributions names all four iterations
- [ ] The Approach Overview header says "four" (bullet list already has all four)
- [ ] Contributions item 1 says "four design iterations"
- [ ] All `\ref{sec:*}` references resolve to sections that exist in the document

---

## Task 1: Fix "three" to "four" in the Abstract (line 181)

<read_first>
- `thesis/main.tex` lines 180-189 (Abstract paragraph)
</read_first>

<action>
Find the exact string in the Abstract:

```
progresses through three design iterations---from a skeleton-graph baseline through distance-transform corridor planning to a lightweight image-space architecture---and
```

Replace with:

```
progresses through four design iterations---from a skeleton-graph baseline through distance-transform corridor planning and a lightweight image-space architecture to a template arc planner with GPS-conditioned turn planning---and
```

This extends the iteration list to include the fourth iteration (template arc + waypoint-turn planner) while keeping the Abstract concise. The em-dash delimited parenthetical now covers all four iterations.
</action>

<acceptance_criteria>
- `grep "four design iterations" thesis/main.tex` matches at least one line in the Abstract region
- `grep "three design iterations" thesis/main.tex` returns no matches anywhere in the file
</acceptance_criteria>

---

## Task 2: Fix "three" to "four" in Approach Overview (line 227)

<read_first>
- `thesis/main.tex` lines 226-234 (Approach Overview iteration list)
</read_first>

<action>
Find the exact string:

```
The system evolved through three design iterations, each backward-compatible with the previous:
```

Replace with:

```
The system evolved through four design iterations, each backward-compatible with the previous:
```

Note: The bullet list below this sentence (Iteration 1 through Iteration 4) already correctly lists all four iterations. Only the header sentence needs the number fix.
</action>

<acceptance_criteria>
- `grep "four design iterations, each" thesis/main.tex` matches exactly one line
</acceptance_criteria>

---

## Task 3: Fix "three" to "four" in Contributions item 1 (line 245)

<read_first>
- `thesis/main.tex` lines 244-246 (Contributions item 1)
</read_first>

<action>
Find the exact string:

```
progressing through three design iterations with backward-compatible improvements.
```

Replace with:

```
progressing through four design iterations with backward-compatible improvements.
```
</action>

<acceptance_criteria>
- `grep "four design iterations with" thesis/main.tex` matches exactly one line
</acceptance_criteria>

---

## Task 4: Fix "three" to "four" in Conclusion (line 1284)

<read_first>
- `thesis/main.tex` lines 1282-1285 (Conclusion Summary of Contributions)
- Note: line numbers will have shifted after Plan 01-01 removed the Ch.4 header (about 6 lines removed, ~2 lines of comments + chapter command + label + blank + intro paragraph). Read the section by searching for `\section{Summary of Contributions}` rather than relying on exact line numbers.
</read_first>

<action>
Find the exact string in the Summary of Contributions section:

```
evolves through three design iterations, from a skeleton-graph baseline through distance-transform corridor planning to a lightweight image-space architecture.
```

Replace with:

```
evolves through four design iterations, from a skeleton-graph baseline through distance-transform corridor planning and a lightweight image-space architecture to a template arc planner with GPS-conditioned turn planning and containment safety verification.
```

This mirrors the Abstract fix, ensuring both the Abstract and Conclusion describe all four iterations consistently.
</action>

<acceptance_criteria>
- `grep "four design iterations, from" thesis/main.tex` matches exactly one line in the Conclusion
- `grep "three design iterations" thesis/main.tex` returns no matches in the entire file
</acceptance_criteria>

---

## Task 5: Final consistency sweep

<read_first>
- `thesis/main.tex` — full file scan for any remaining "three" that incorrectly refers to design iterations
</read_first>

<action>
Search the entire file for the word "three" (case-insensitive). For each occurrence, verify it does NOT refer to the number of design iterations. Per the research, these occurrences are safe and should NOT be changed:

- `\usepackage{threeparttable}` — LaTeX package (keep)
- `\begin{threeparttable}` / `\end{threeparttable}` — LaTeX environment (keep)
- "over three meters" — sidewalk width (keep)
- "three operating in BEV space" — planner count, correct as-is (keep)
- "across three campus frames" — figure caption (keep)
- "under three configurations" — overnight eval (keep)

If any NEW occurrence of "three" referring to design iterations is found (not in the list above), fix it to "four". Otherwise, no changes needed in this task.

Also verify:
- No `\ref{ch:closed_loop}` remains
- No `\ref{ch:related_work}` remains
- No `\ref{ch:methodology}` remains (section refs like `\ref{sec:*}` are fine)
- No `\ref{ch:results}` remains
- The string "Chapter 4" does not appear literally in the text (LaTeX should use `Chapter~\ref{ch:evaluation}` not hardcoded numbers)
</action>

<acceptance_criteria>
- `grep -c "three design iteration" thesis/main.tex` returns 0
- `grep -c "\\\\ref{ch:closed_loop}" thesis/main.tex` returns 0
- `grep -c "\\\\ref{ch:related_work}" thesis/main.tex` returns 0
- `grep -c "\\\\ref{ch:methodology}" thesis/main.tex` returns 0
- `grep -c "\\\\ref{ch:results}" thesis/main.tex` returns 0
- Total count of `\chapter{` (non-starred) is exactly 6
</acceptance_criteria>

---

## Verification

After all tasks, the thesis should satisfy:
1. "four design iterations" appears in: Abstract, Approach Overview, Contributions item 1, Conclusion
2. "three design iterations" appears nowhere
3. All chapter labels are the new set: `ch:introduction`, `ch:background`, `ch:system_design`, `ch:evaluation`, `ch:discussion`, `ch:conclusion`
4. All `\ref{ch:*}` use only the new label set
5. The document has exactly 6 numbered chapters

---

*Plan: 01-02 | Phase: 01-structural-reorganization*
*Created: 2026-03-30*
