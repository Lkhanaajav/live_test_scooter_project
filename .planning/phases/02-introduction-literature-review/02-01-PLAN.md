---
phase: 02-introduction-literature-review
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [thesis/main.tex]
autonomous: true
requirements: [NARR-01]

must_haves:
  truths:
    - "Introduction opens with a concrete sidewalk scenario, not a generic 'growing field' statement"
    - "Problem statement frames two open questions: planning domain choice and robust BEV for turns"
    - "Contributions list has 4-5 items centered on two key findings"
    - "Tone is measured and hedged, not assertive conference-paper style"
    - "Figure fig:scooter_hw is preserved at end of chapter"
  artifacts:
    - path: "thesis/main.tex"
      provides: "Rewritten Chapter 1 Introduction"
      contains: "\\chapter{Introduction}"
  key_links:
    - from: "thesis/main.tex Introduction"
      to: "thesis/main.tex Chapter 4 Evaluation"
      via: "Contribution items reference evaluation sections"
      pattern: "\\\\ref\\{ch:evaluation\\}"
    - from: "thesis/main.tex Introduction"
      to: "thesis/main.tex Chapter 2 Background"
      via: "Thesis Organization paragraph"
      pattern: "\\\\ref\\{ch:background\\}"
---

<objective>
Rewrite Chapter 1 (Introduction) of thesis/main.tex with a compelling narrative structure per NARR-01.

Purpose: The Introduction frames the entire thesis. It must open with a concrete scenario hook (D-01), establish the two key findings (D-03), consolidate contributions from 7 to 4-5 items (D-04), and use measured thesis tone (D-09/D-10). The nuanced BEV argument (D-05) must be present: BEV is essential for corridor extraction and turns, but image-space dominates for straight-ahead following.

Output: Rewritten Introduction chapter (lines 197-283 of thesis/main.tex) growing from ~90 lines to ~130-180 lines.
</objective>

<execution_context>
@C:/Users/miji0000/Desktop/thesis_prep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miji0000/Desktop/thesis_prep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-introduction-literature-review/02-CONTEXT.md
@.planning/phases/02-introduction-literature-review/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Motivation and Problem Statement sections</name>
  <files>thesis/main.tex</files>
  <read_first>
    - thesis/main.tex (lines 197-283 for current Introduction; lines 370-390 for System Design chapter opening that references Introduction; lines 1192-1260 for Discussion/Conclusion that reference contributions)
    - .planning/phases/02-introduction-literature-review/02-CONTEXT.md (locked decisions D-01 through D-05, D-09, D-10)
    - .planning/phases/02-introduction-literature-review/02-RESEARCH.md (Architecture Patterns section for target structure; Key Data Points table; Hedging Vocabulary)
  </read_first>
  <action>
Replace the current \section{Motivation} (lines 201-207) and \section{Problem Statement} (lines 210-220) with rewritten versions following the target structure from 02-RESEARCH.md.

**Motivation section (3 paragraphs):**

Paragraph 1 — CONCRETE SCENARIO HOOK (per D-01, D-02):
Open with a specific physical scenario of a battery-powered scooter or delivery robot on a university sidewalk. Describe the cracked asphalt, no lane markings, no curb paint, commodity RGB camera, single-board ARM processor under fifteen watts. End with the intellectual question: how should such a platform perceive the traversable corridor and plan a safe trajectory? Use the pattern from 02-RESEARCH.md "Concrete Scenario Hook Pattern" code example. This must NOT start with "Autonomous driving is a growing field" or any variant — it must start with the physical scene.

Paragraph 2 — BROADER CONTEXT:
Why sidewalk navigation matters (economic: last-mile delivery costs; social: assistive mobility; accessibility: campus shuttles). Why monocular is the right constraint (cost under $50, power under 15W, form factor). Cite \cite{starship2022} for delivery robots. Keep existing content themes but rewrite prose to flow from the hook.

Paragraph 3 — LIMITATIONS OF EXISTING APPROACHES:
End-to-end approaches are black-box and need GPU. Learned BEV methods (LSS, BEVFormer) assume multi-camera and GPU. Even classical BEV skeleton-graph planning is fragile with monocular input — the BEV grid receives only a narrow strip from a single camera, causing downstream planners to fail. Cite \cite{e2e_nav_policies}, \cite{zhao2024bev}, \cite{lift_splat_shoot}, \cite{bevformer}. This sets up the need for a new approach. Per D-05, do NOT say "BEV is bad" — say BEV skeleton-graph construction from noisy monocular projections is fragile.

**Problem Statement section (2 paragraphs):**

Paragraph 1 — FORMAL PROBLEM SPECIFICATION:
State the problem: single monocular camera -> traversable region segmentation -> centered path -> safe control commands. Frame as TWO open questions (per D-02):
  Q1: For straight-ahead path following, which planning domain yields better accuracy and efficiency — bird's-eye view or image-space?
  Q2: When BEV-domain reasoning is needed (corridor validation, turn planning), how can it be made robust against monocular projection fragility?

Paragraph 2 — SCOPE AND CONSTRAINTS:
Offline evaluation on pre-recorded campus sidewalk video. CPU-only timing measurements to reflect embedded deployment. Six video sequences totaling over 22,000 frames. No closed-loop vehicle control in evaluation (isolates perception and planning).

**Tone (per D-09, D-10):** Use hedged, measured language. "This thesis investigates..." not "We demonstrate...". Passive voice acceptable. Conservative for OU committee.

**Preserve:** All existing \label{} identifiers. Do not rename \section{Motivation} or \section{Problem Statement}. Keep the enumerated list format in Problem Statement ONLY if it fits naturally; otherwise convert to prose paragraphs with the two questions.

**Do NOT touch:** Anything below \section{Approach Overview} (line 223+). This task only modifies Motivation and Problem Statement.
  </action>
  <verify>
    <automated>grep -c "Consider a\|consider a\|scooter\|sidewalk.*camera\|cracked" thesis/main.tex | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for "Consider a" or similar concrete scenario opening in the Motivation section returns at least 1 match
    - grep for "growing field" or "recent advances" in the Motivation section returns 0 matches
    - The two open questions (planning domain choice AND robust BEV for turns) appear in Problem Statement
    - grep for "We demonstrate" or "We show that" or "Our approach" in the modified sections returns 0 matches (hedged tone per D-09)
    - \section{Motivation} and \section{Problem Statement} labels preserved
    - No content below line 222 (Approach Overview and beyond) is modified
  </acceptance_criteria>
  <done>Motivation opens with concrete sidewalk scenario, Problem Statement frames two research questions, tone is measured/hedged, no assertive conference-paper language</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Approach Overview, Contributions, and Thesis Organization sections</name>
  <files>thesis/main.tex</files>
  <read_first>
    - thesis/main.tex (lines 197-283 for full Introduction — must re-read after Task 1 changes; lines 717-730 for Evaluation chapter section labels needed for contribution forward-references; lines 1254-1300 for Conclusion contribution mapping)
    - .planning/phases/02-introduction-literature-review/02-CONTEXT.md (D-03 two findings, D-04 consolidate to 4-5, D-05 nuanced BEV argument)
    - .planning/phases/02-introduction-literature-review/02-RESEARCH.md (Contribution Item Pattern; Key Data Points table with exact numbers)
  </read_first>
  <action>
Replace \section{Approach Overview} (current lines ~223-237), \section{Contributions} (current lines ~240-258), and \section{Thesis Organization} (current lines ~261-263) with rewritten versions. Preserve the figure block (lines 266-283) — do NOT move or modify the fig:scooter_hw figure.

**Approach Overview section (3 paragraphs):**

Paragraph 1 — PIPELINE AT A GLANCE:
Modular pipeline: segmentation (SegFormer-B0 via teacher-student training) -> optional BEV projection (calibrated homography) -> path extraction (one of five methods across two domains) -> temporal smoothing -> control output. Keep existing content but tighten prose. Cite \cite{segformer}, \cite{oneformer}.

Paragraph 2 — DESIGN ITERATION NARRATIVE:
Four iterations, one sentence each. Frame the key story as evolution from "discovery" (constructing paths from noisy pixels) to "verification" (scoring pre-computed templates against a corridor). Per D-05: the final system still uses BEV for corridor extraction and turn planning, but straight-ahead path following moved to image-space. Keep the existing \begin{itemize} list format with the four iterations (already correct from Phase 1).

Paragraph 3 — POSITIONING VS RELATED WORK:
Brief paragraph distinguishing from end-to-end steering policies (GPU, black-box), dense monocular BEV reconstruction (multi-camera assumption), and classic mask corridor following (no intersection topology). This thesis is monocular, transparent, CPU-friendly, and provides the first BEV-vs-image-space planning comparison for sidewalks. Rewrite existing paragraph with hedged tone.

**Contributions section — CONSOLIDATE FROM 7 TO 5 ITEMS (per D-03, D-04):**

Replace the current 7-item \begin{enumerate} with exactly 5 items:

1. **Systematic BEV-vs-image-space comparison (Finding 1 per D-03):** A systematic comparison of five path-planning methods across BEV and image-space domains, providing evidence that image-space midpoint planning achieves approximately 14.3 px lateral error at 2.2 ms versus 65.0 px at 926.8 ms for the best BEV method (Chapter~\ref{ch:evaluation}). Use hedged language per D-09.

2. **Template-approval architecture (Finding 2 per D-03):** A template-approval planning architecture that replaces fragile skeleton-graph construction with efficient verification of pre-computed arc templates against distance-transform corridors, reducing per-frame planning cost from approximately 109 ms to 69 ms while eliminating noise-sensitive graph construction (Chapter~\ref{ch:system_design}). Per D-05: emphasize this uses BEV corridors for verification, not discovery.

3. **Teacher-student segmentation (supporting):** A semi-supervised segmentation training recipe using OneFormer Swin-L as teacher and SegFormer-B0 as student, achieving IoU of 0.946 at 11.7 ms on CPU (Chapter~\ref{ch:evaluation}). Cite \cite{oneformer}, \cite{segformer}.

4. **Comprehensive evaluation protocol (supporting):** A comprehensive offline evaluation protocol covering segmentation quality, planner accuracy, temporal stability, runtime analysis, and an 1800-frame accepted run across six campus video sequences totaling over 22,000 frames (Chapter~\ref{ch:evaluation}).

5. **GPS-conditioned turn planner with safety guard (supporting):** A GPS-conditioned waypoint-turn planner with a containment safety guard that verifies committed paths against the BEV corridor before execution, validated with 0% containment failure rate (Chapter~\ref{ch:system_design}, Chapter~\ref{ch:evaluation}).

Each item must use hedged language (per D-09): "providing evidence that..." not "demonstrating that...", "achieving" is acceptable for measured results.

**Thesis Organization section:**
Keep as a single paragraph. Update to reference the correct 6 chapter names with their labels:
- Chapter~\ref{ch:background}: reviews prior work and identifies research gaps
- Chapter~\ref{ch:system_design}: describes the complete system design
- Chapter~\ref{ch:evaluation}: presents experimental results
- Chapter~\ref{ch:discussion}: discusses implications, limitations, threats to validity
- Chapter~\ref{ch:conclusion}: summarizes contributions and future work

Add a bridge sentence at the end referencing Figure~\ref{fig:scooter_hw} to connect to the figure that follows.

**CRITICAL:** Do NOT modify the figure block (lines 266-283 approximately). It must remain exactly as-is after the Thesis Organization section.
  </action>
  <verify>
    <automated>grep -c "\\\\item" thesis/main.tex | head -1</automated>
  </verify>
  <acceptance_criteria>
    - The Contributions enumerate block contains exactly 5 \item entries (grep "\\item" within the Contributions section)
    - Contribution 1 contains "five path-planning" and references Finding 1 data: "14.3" and "926.8" or "421"
    - Contribution 2 contains "template" and "corridor" and references the discovery-to-verification evolution
    - Contribution 2 does NOT say "BEV is fragile" or "BEV is bad" — it says BEV corridors are used for verification (per D-05)
    - Contribution 3 contains "OneFormer" and "SegFormer" and "0.946"
    - Contribution 5 contains "containment" and "0%"
    - grep for "We demonstrate" or "We show" or "We prove" in the Contributions section returns 0 matches
    - \label{fig:scooter_hw} figure block is present and unchanged after the Thesis Organization section
    - Thesis Organization references all 5 chapter labels: ch:background, ch:system_design, ch:evaluation, ch:discussion, ch:conclusion
    - The four design iterations list is preserved in Approach Overview (Iteration 1 through Iteration 4)
  </acceptance_criteria>
  <done>Contributions consolidated to 5 items centered on two findings, Approach Overview tells the discovery-to-verification story, Thesis Organization updated for 6-chapter structure, figure preserved, hedged tone throughout</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. grep -c "\\\\chapter{Introduction}" thesis/main.tex returns 1
2. grep -c "\\\\label{ch:introduction}" thesis/main.tex returns 1
3. grep -c "\\\\section{Motivation}" thesis/main.tex returns 1
4. grep -c "\\\\section{Problem Statement}" thesis/main.tex returns 1
5. grep -c "\\\\section{Approach Overview}" thesis/main.tex returns 1
6. grep -c "\\\\section{Contributions}" thesis/main.tex returns 1
7. grep -c "\\\\section{Thesis Organization}" thesis/main.tex returns 1
8. grep -c "\\\\label{fig:scooter_hw}" thesis/main.tex returns 1
9. No occurrences of "We demonstrate" or "We show that" or "Our approach achieves" in Chapter 1
10. The word "four design iterations" appears in the Approach Overview
</verification>

<success_criteria>
- Introduction opens with a concrete sidewalk scenario (not generic autonomous driving framing)
- Two research questions clearly stated in Problem Statement
- Contributions consolidated from 7 to exactly 5 items
- Two key findings (BEV-vs-image-space comparison, template-approval architecture) are items 1 and 2
- Nuanced BEV argument present: BEV used for corridor verification, not dismissed entirely (per D-05)
- Measured, hedged thesis tone throughout (per D-09, D-10)
- All existing cross-reference labels preserved
- Figure fig:scooter_hw block unchanged at end of chapter
- Chapter 2 (Background) content NOT modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-introduction-literature-review/02-01-SUMMARY.md`
</output>
