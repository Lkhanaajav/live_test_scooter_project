---
phase: 11.1-gps-intent-corridor-waypoint-turn-planner
plan: "04"
type: execute
wave: 4
depends_on:
  - "11.1-03"
files_modified:
  - .planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-REPLAY_SET.txt
  - config.py
  - live_heading_demo.py
  - scripts/eval_waypoint_turn_planner.py
  - tests/test_realtime_nav_core.py
  - tests/test_waypoint_turn_planner.py
  - waypoint_turn_planner.py
autonomous: true
requirements:
  - WPT-02
  - WPT-03
  - WPT-04

must_haves:
  truths:
    - "The repo can replay and compare baseline, Phase 11 template, and Phase 11.1 waypoint-turn behavior on the same recordings"
    - "Waypoint-turn threshold tuning is driven by replay metrics instead of visual guesswork alone"
    - "Low-confidence/hold rate, maneuver-family stability, and heading behavior are measurable from evaluation output"
    - "A saved three-mode replay comparison artifact exists for a fixed replay set, so phase sign-off cannot rely on `--help` output alone"
  artifacts:
    - path: "scripts/eval_waypoint_turn_planner.py"
      provides: "three-mode replay/evaluation harness for baseline, template, and waypoint-turn planners"
      contains: "waypoint_turn_rate"
    - path: ".planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-REPLAY_SET.txt"
      provides: "fixed replay manifest used for phase acceptance runs"
      contains: ".mp4|.mov"
    - path: "tests/test_waypoint_turn_planner.py"
      provides: "smoke coverage for replay summaries and waypoint-turn evaluation outputs"
      min_lines: 260
    - path: "config.py"
      provides: "documented accepted waypoint-turn thresholds used by replay runs"
      contains: "WAYPOINT"
  key_links:
    - from: "scripts/eval_waypoint_turn_planner.py"
      to: "live_heading_demo.py"
      via: "shared runtime flags for planner mode selection"
      pattern: "run_live|planner_mode|waypoint"
    - from: "scripts/eval_waypoint_turn_planner.py"
      to: "waypoint_turn_planner.py"
      via: "replay metrics summarized from waypoint-turn diagnostics"
      pattern: "support|hold|confidence"
---

<objective>
Add the replay comparison and threshold-tuning layer for Phase 11.1.

Purpose: make the new waypoint-turn mode measurable against the current baseline and Phase 11 template mode before thesis-facing validation uses it.
Output: replay evaluator, summary metrics, and documented first-pass threshold values for waypoint-turn behavior.
</objective>

<execution_context>
@C:/Users/miji0000/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/miji0000/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-RESEARCH.md
@.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-VALIDATION.md

@config.py
@live_heading_demo.py
@scripts/eval_template_planner.py
@waypoint_turn_planner.py
@tests/test_waypoint_turn_planner.py

Current Phase 11 replay helper already compares graph baseline vs template mode. Phase 11.1 should preserve that tooling and add a waypoint-turn comparison path rather than overwriting the Phase 11 script.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a replay evaluator for waypoint-turn comparison metrics</name>
  <files>.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-REPLAY_SET.txt, scripts/eval_waypoint_turn_planner.py, tests/test_waypoint_turn_planner.py, tests/test_realtime_nav_core.py</files>
  <behavior>
    - Test 1: the new evaluation script can summarize baseline, template, and waypoint-turn runs without requiring a live camera
    - Test 2: replay summaries include waypoint-turn-specific metrics such as hold rate, confidence rate, maneuver-family switches, and path-source counts
    - Test 3: the summary parser remains robust when some runs lack waypoint-turn fields
    - Test 4: runtime mode dispatch proves the evaluator actually exercises three distinct planner modes rather than replaying the same selector three times
  </behavior>
  <action>
    Create `scripts/eval_waypoint_turn_planner.py` as a Phase 11.1-specific replay helper. Reuse the summary/reporting pattern from `scripts/eval_template_planner.py`, but keep the Phase 11 script intact.

    The new evaluator must compare at least three runtime modes on the same recordings:
    - current baseline graph/dt behavior
    - Phase 11 template approval
    - Phase 11.1 waypoint-turn mode

    Report metrics that matter for WPT validation:
    - `path_source` mix
    - waypoint-turn rate / hold rate
    - low-confidence rate
    - maneuver-family switch count
    - target-support or corridor-support statistics
    - mean / p95 heading magnitude

    Also create `.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-REPLAY_SET.txt` as the fixed replay manifest used for acceptance. The evaluator must accept that manifest and write per-mode outputs plus a combined comparison artifact (JSON, CSV, or Markdown) under a deterministic output directory.
  </action>
  <verify>
    <automated>python -m pytest tests/test_realtime_nav_core.py -q -k "planner_mode or waypoint_turn" && python scripts/eval_waypoint_turn_planner.py --replay-set .planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-REPLAY_SET.txt --modes baseline template waypoint_turn --output-dir outputs/evaluation/waypoint_turn_phase11_1 && python -m pytest tests/test_waypoint_turn_planner.py -q -k "replay or summary"</automated>
  </verify>
  <done>The repo has a dedicated replay/evaluation harness for Phase 11.1, a fixed replay manifest, and a saved three-mode comparison artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Tune and document waypoint-turn thresholds using replay metrics</name>
  <files>config.py, live_heading_demo.py, waypoint_turn_planner.py, scripts/eval_waypoint_turn_planner.py</files>
  <action>
    Use representative left-turn, right-turn, and ambiguous-turn replays to set the first accepted Phase 11.1 values for:
    - decision-band placement
    - acquire and sustain support thresholds
    - path containment thresholds
    - low-confidence and hold thresholds
    - slowdown floors during unsupported turns

    Keep changes constrained to waypoint-turn mode. Do not widen scope into controller replacement, segmentation retraining, or deletion of the Phase 11 template path.

    Document the chosen thresholds in code comments or evaluator output so future comparisons can replay the exact accepted settings. Record the acceptance metrics for the fixed replay set in `11.1-04-SUMMARY.md`, including `path_source` mix, hold rate, low-confidence rate, maneuver-family switches, and heading statistics for baseline, template, and waypoint-turn modes.
  </action>
  <verify>
    <automated>python scripts/eval_waypoint_turn_planner.py --replay-set .planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-REPLAY_SET.txt --modes baseline template waypoint_turn --output-dir outputs/evaluation/waypoint_turn_phase11_1 && python -m pytest tests -q</automated>
  </verify>
  <done>Phase 11.1 has a repeatable three-mode evaluation path, replay-backed threshold values, and a saved acceptance artifact for commanded-turn behavior.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_realtime_nav_core.py -q -k "planner_mode or waypoint_turn"`
2. `python scripts/eval_waypoint_turn_planner.py --replay-set .planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-REPLAY_SET.txt --modes baseline template waypoint_turn --output-dir outputs/evaluation/waypoint_turn_phase11_1`
3. `python -m pytest tests/test_waypoint_turn_planner.py -q -k "replay or summary"`
4. `python -m pytest tests -q`
</verification>

<success_criteria>
- WPT-02: replay evidence shows the waypoint-turn path stays smoother and more corridor-faithful than the ad hoc manual/skeleton fallback
- WPT-03: maneuver-family stability is measurable from replay summaries
- WPT-04: low-confidence and hold behavior are explicit, measurable, and preferred over wrong-maneuver approval
- Phase acceptance requires a saved comparison artifact for the fixed three-mode replay set
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-04-SUMMARY.md`
</output>
