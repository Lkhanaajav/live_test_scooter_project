---
phase: 11.1-gps-intent-corridor-waypoint-turn-planner
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bev_obstacle.py
  - template_path_planner.py
  - waypoint_turn_planner.py
  - tests/conftest.py
  - tests/test_bev_obstacle.py
  - tests/test_template_path_planner.py
  - tests/test_waypoint_turn_planner.py
autonomous: true
requirements:
  - WPT-01
  - WPT-02

must_haves:
  truths:
    - "Phase 11.1 starts from an honest green baseline instead of building on pre-existing red tests"
    - "Wave 0 waypoint-turn fixtures and contract tests exist before runtime integration begins"
    - "Phase 11 template approval behavior remains intact while Phase 11.1 scaffolding is added"
  artifacts:
    - path: "waypoint_turn_planner.py"
      provides: "Wave 0 public contract stub for the waypoint-turn planner"
      contains: "plan_waypoint_turn"
    - path: "tests/test_waypoint_turn_planner.py"
      provides: "Phase 11.1 contract and fixture coverage"
      min_lines: 120
    - path: "template_path_planner.py"
      provides: "repaired Phase 11 straight-corridor baseline behavior"
      contains: "corridor_from_mask"
  key_links:
    - from: "tests/test_waypoint_turn_planner.py"
      to: "waypoint_turn_planner.py"
      via: "imported public contract and Wave 0 smoke expectations"
      pattern: "plan_waypoint_turn|WaypointTurnPlannerConfig"
    - from: "tests/test_template_path_planner.py"
      to: "template_path_planner.py"
      via: "straight corridor extraction and approval regression coverage"
      pattern: "corridor_from_mask|approve_template_bank"
---

<objective>
Create the Wave 0 preconditions for Phase 11.1.

Purpose: on March 23, 2026, `python -m pytest tests -q` is already red before Phase 11.1 begins, and the new waypoint-turn work needs its own test/contract scaffold without disturbing Phase 11 template approval behavior.
Output: repaired baseline tests, `waypoint_turn_planner.py` contract stub, and synthetic fixtures/tests for Phase 11.1.
</objective>

<execution_context>
@C:/Users/miji0000/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/miji0000/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-CONTEXT.md
@.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-RESEARCH.md
@.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-VALIDATION.md

@template_path_planner.py
@bev_obstacle.py
@tests/test_template_path_planner.py
@tests/test_bev_obstacle.py
@tests/conftest.py

Baseline on 2026-03-23:
- `tests/test_bev_obstacle.py::test_metric_conversion` fails
- `tests/test_template_path_planner.py::test_corridor_from_mask_extracts_centered_bounds` fails
- `tests/test_template_path_planner.py::test_straight_corridor_prefers_straight_template` fails

This plan must repair those failures without introducing Phase 11.1 runtime behavior yet.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 waypoint-turn fixtures and contract tests</name>
  <files>waypoint_turn_planner.py, tests/conftest.py, tests/test_waypoint_turn_planner.py</files>
  <behavior>
    - Test 1: commanded-left and commanded-right fixtures expose distinct commanded-side support clusters inside a forward decision band
    - Test 2: unsupported or fragmented commanded-turn masks return a low-confidence or hold-style result from the public waypoint-turn contract
    - Test 3: no-intent and straight-intent inputs leave the commanded-turn module inactive rather than silently selecting a turn
  </behavior>
  <action>
    Create `tests/test_waypoint_turn_planner.py` and extend `tests/conftest.py` with synthetic masks for commanded-left, commanded-right, false-pocket, unsupported-turn, and no-intent cases.

    Add a minimal `waypoint_turn_planner.py` contract stub that exports the public interfaces the later plans will implement, including a config dataclass, target/result dataclasses, and a top-level `plan_waypoint_turn(...)`. The stub may return empty or low-confidence placeholder results, but the imports and function signatures must be stable and testable.

    Keep this scaffold additive. Do not call the new module from `realtime_nav_core.py` in this plan.
  </action>
  <verify>
    <automated>python -m pytest tests/test_waypoint_turn_planner.py -q -k "contract or fixture or inactive"</automated>
  </verify>
  <done>Phase 11.1 has a stable Wave 0 test/contract scaffold and the new module imports cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the pre-phase red baseline without changing planner mode semantics</name>
  <files>bev_obstacle.py, template_path_planner.py, tests/test_bev_obstacle.py, tests/test_template_path_planner.py</files>
  <behavior>
    - Test 1: `tests/test_bev_obstacle.py::test_metric_conversion` matches the analytical homography fixture used by the obstacle tests
    - Test 2: `tests/test_template_path_planner.py::test_corridor_from_mask_extracts_centered_bounds` recenters a straight corridor around the calibrated ego position
    - Test 3: `tests/test_template_path_planner.py::test_straight_corridor_prefers_straight_template` approves the straight template in a centered straight corridor
  </behavior>
  <action>
    Fix the existing baseline regressions in `bev_obstacle.py` and `template_path_planner.py` so the repo is green before commanded-turn work starts.

    Keep the fixes narrow and reversible:
    - preserve Phase 11 template approval as the active behavior
    - do not add waypoint-turn runtime wiring here
    - do not delete or rewrite existing Phase 11/template code paths

    If the issue is in the synthetic fixture assumptions rather than the production formula, make the smallest explainable correction in the associated test file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_bev_obstacle.py -q && python -m pytest tests/test_template_path_planner.py -q && python -m pytest tests -q</automated>
  </verify>
  <done>The full test suite is green again and Phase 11.1 starts from a truthful, reproducible baseline.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_waypoint_turn_planner.py -q -k "contract or fixture or inactive"`
2. `python -m pytest tests/test_bev_obstacle.py -q`
3. `python -m pytest tests/test_template_path_planner.py -q`
4. `python -m pytest tests -q`
</verification>

<success_criteria>
- Wave 0 exists as a real plan, not an implied prerequisite
- The March 23, 2026 baseline failures are fixed or corrected in-place before Phase 11.1 implementation begins
- Phase 11.1 has its own contract/fixture scaffold without altering the active Phase 11 runtime selector
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-01-SUMMARY.md`
</output>
