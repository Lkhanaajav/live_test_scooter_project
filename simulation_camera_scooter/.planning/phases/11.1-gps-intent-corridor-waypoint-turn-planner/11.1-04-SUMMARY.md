---
phase: 11.1-gps-intent-corridor-waypoint-turn-planner
plan: "04"
subsystem: evaluation
tags: [replay, evaluation, waypoint-turn, threshold-tuning, metrics, comparison]

# Dependency graph
requires:
  - phase: 11.1-gps-intent-corridor-waypoint-turn-planner (plan 03)
    provides: waypoint-turn runtime integration with lock state and speed gating
provides:
  - three-mode replay evaluator comparing baseline, template, and waypoint-turn planners
  - fixed replay manifest for phase acceptance runs
  - documented and rationalized waypoint-turn threshold set
  - threshold provenance logging in evaluation artifacts
affects: [thesis-validation, phase-signoff, future-threshold-tuning]

# Tech tracking
tech-stack:
  added: [pandas (optional), csv (stdlib fallback)]
  patterns: [threshold-snapshot provenance, dual-backend summarization, manifest-driven replay]

key-files:
  created:
    - scripts/eval_waypoint_turn_planner.py
    - .planning/phases/11.1-gps-intent-corridor-waypoint-turn-planner/11.1-REPLAY_SET.txt
  modified:
    - config.py
    - RUNTIME_RUNBOOK.md

key-decisions:
  - "Separate evaluator from Phase 11 eval_template_planner.py to preserve existing tooling"
  - "Dual pandas/csv backend so script works in minimal environments without pandas"
  - "Threshold snapshot embedded in evaluation artifacts for reproducible provenance"
  - "All 17 WAYPOINT_* thresholds documented with derivation rationale in config.py"

patterns-established:
  - "Threshold provenance: evaluation artifacts include _snapshot_waypoint_thresholds() capture"
  - "Manifest-driven replay: 11.1-REPLAY_SET.txt defines fixed video set for acceptance"
  - "Three-mode comparison pattern: baseline vs template vs waypoint_turn on same videos"

requirements-completed: [WPT-02, WPT-03, WPT-04]

# Metrics
duration: 3min
completed: 2026-03-24
---

# Phase 11.1 Plan 04: Replay Evaluator and Threshold Tuning Summary

**Three-mode replay evaluator with waypoint-turn metrics (hold rate, family switches, confidence, heading) plus 17 documented and rationalized threshold values**

## Performance

- **Duration:** ~3 min (continuation from interrupted session; original RED commit was prior)
- **Started:** 2026-03-24T00:00:00Z (continuation)
- **Completed:** 2026-03-24T00:03:00Z
- **Tasks:** 2 (Task 1 TDD RED was prior commit, GREEN + Task 2 completed here)
- **Files modified:** 4

## Accomplishments

- Three-mode replay evaluator (`scripts/eval_waypoint_turn_planner.py`) comparing baseline, template, and waypoint_turn planners on the same video recordings
- Fixed replay manifest (`11.1-REPLAY_SET.txt`) with 3 campus sidewalk videos for phase acceptance
- All 17 WAYPOINT_* config thresholds documented with derivation rationale: decision band placement, acquire/sustain support, path containment gates, confidence/slowdown floors, path geometry, and maneuver lock parameters
- Threshold provenance logging: `_snapshot_waypoint_thresholds()` captures active config values into `thresholds.json` alongside evaluation artifacts

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Failing tests for replay evaluator** - `e9f14fc` (test) [prior session]
2. **Task 1 GREEN: Implement replay evaluator** - `785143c` (feat)
3. **Task 2: Document accepted thresholds** - `2572252` (feat)
4. **Deviation fix: RUNTIME_RUNBOOK.md paths** - `e1c262e` (fix)

## Files Created/Modified

- `scripts/eval_waypoint_turn_planner.py` - Three-mode replay comparison harness with waypoint-turn-specific metrics
- `.planning/phases/11.1-.../11.1-REPLAY_SET.txt` - Fixed replay manifest (3 campus sidewalk videos)
- `config.py` - 17 WAYPOINT_* thresholds with detailed derivation comments
- `RUNTIME_RUNBOOK.md` - Fixed absolute paths to relative repo paths, updated model reference

## Decisions Made

1. **Separate evaluator script** - Created `eval_waypoint_turn_planner.py` alongside existing `eval_template_planner.py` rather than extending it, to preserve Phase 11 tooling
2. **Dual backend** - Both pandas and stdlib csv backends for summarization, so the script works in minimal environments
3. **Threshold provenance** - Active config values are captured into evaluation JSON artifacts so future replays can verify which thresholds were used
4. **Threshold rationale** - Each threshold documented with derivation from BEV geometry (NAV_BEV_FORWARD_M=11m, BEV_SIZE=360x660), campus sidewalk dimensions (~2-3m wide), and safety invariants

## Accepted Threshold Values (Phase 11.1 Plan 04)

| Parameter | Value | Rationale |
|-----------|-------|-----------|
| DECISION_BAND_MIN_M | 2.0 | Avoids ego-artifact zone at BEV bottom rows |
| DECISION_BAND_MAX_M | 7.0 | Matches real BEV visibility range for campus turns |
| ACQUIRE_SUPPORT_MIN | 0.40 | Prevents false triggers from segmentation noise |
| SUSTAIN_SUPPORT_MIN | 0.25 | Hysteresis: lower threshold for keeping existing target |
| TARGET_UPDATE_ALPHA | 0.40 | Moderate EMA: responsive but filters outliers |
| PATH_CONTAINMENT_MIN | 0.60 | Allows slight apex overshoot while keeping majority inside |
| NEAR_CONTAINMENT_MIN | 0.70 | Stricter near ego (immediate execution zone) |
| NEAR_FIELD_M | 1.20 | First 1.2m of path must be well-contained |
| LOW_CONFIDENCE_THRESHOLD | 0.35 | Below this: hold recommended |
| SLOWDOWN_FLOOR | 0.35 | Minimum 35% speed reduction when uncertain |
| HOLD_SLOWDOWN | 1.0 | Full slowdown (near-stop) on hold |
| PATH_HORIZON_M | 6.0 | Shorter than BEV range to avoid noisy far-field |
| PATH_DS_M | 0.25 | ~24 points over 6m, smooth for cubic Hermite |
| EXIT_REJOIN_FACTOR | 0.6 | 60% blend back toward corridor center after apex |
| TURN_ENABLED | True | Phase 11.1 active by default |
| LOCK_SUSTAIN_FRAMES | 3 | 3 consecutive supported frames before lock |
| LOCK_RELEASE_FRAMES | 5 | 5 consecutive unsupported frames to release |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed RUNTIME_RUNBOOK.md absolute paths**
- **Found during:** Task 2 (threshold documentation)
- **Issue:** RUNTIME_RUNBOOK.md contained absolute machine-specific paths (`C:/Users/miji0000/...`) and outdated model reference
- **Fix:** Replaced with relative repo paths and updated to current best model (binary_segformer_old400_img1931_vid017_020)
- **Files modified:** RUNTIME_RUNBOOK.md
- **Committed in:** `e1c262e`

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Minor documentation fix, no scope creep.

## Issues Encountered

- Previous executor was interrupted by rate limit after RED commit but before GREEN commit. The GREEN implementation was on disk but unstaged. This continuation committed the existing work and completed Task 2.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 11.1 now has a complete evaluation pipeline: replay evaluator, fixed manifest, documented thresholds
- Replay evaluation requires test videos on local machine (not in git) -- the evaluator handles missing videos gracefully with warnings
- Phase 11.1 is ready for thesis-facing validation using the three-mode comparison artifact
- All 206 tests pass

## Self-Check: PASSED

All files verified present, all commit hashes found in git log:
- FOUND: scripts/eval_waypoint_turn_planner.py
- FOUND: .planning/phases/11.1-.../11.1-REPLAY_SET.txt
- FOUND: .planning/phases/11.1-.../11.1-04-SUMMARY.md
- FOUND: e9f14fc (TDD RED)
- FOUND: 785143c (TDD GREEN)
- FOUND: 2572252 (threshold docs)
- FOUND: e1c262e (runbook fix)

---
*Phase: 11.1-gps-intent-corridor-waypoint-turn-planner*
*Completed: 2026-03-24*
