---
phase: 03.1-yolo-bev-obstacle-projection
plan: "03.1-04"
subsystem: visualization

tags: [opencv, bev, obstacle, visualization, draw_bev_hud]

# Dependency graph
requires:
  - phase: 03.1-03
    provides: obstacle_zones list built in live_heading_demo and passed to planner

provides:
  - draw_bev_hud() extended with obstacle_zones_m optional parameter
  - Orange circles (approaching, fwd_m >= BEV_HARD_BLOCK_DIST_M) drawn on BEV overlay
  - Red circles (stop zone, fwd_m < BEV_HARD_BLOCK_DIST_M) drawn on BEV overlay
  - Backward-compatible: existing callers unaffected when obstacle_zones_m=None

affects:
  - Phase 02 BEV calibration (BEV overlay now shows obstacle geometry)
  - Phase 03 Demo Integration (BEV HUD is the primary live feedback panel)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Optional visualization param pattern: add keyword arg =None, skip drawing block if falsy"
    - "Metric-to-pixel: col = (lat_m/NAV_BEV_LATERAL_M + 0.5)*(w_bev-1), row = (1 - fwd_m/NAV_BEV_FORWARD_M)*(h_bev-1)"
    - "Color coding by distance threshold: red if fwd_m < BEV_HARD_BLOCK_DIST_M else orange"

key-files:
  created: []
  modified:
    - simulation_camera_scooter/visualization.py
    - simulation_camera_scooter/live_heading_demo.py

key-decisions:
  - "obstacle_zones_m is optional (=None default) so headless mode and existing callers are unaffected"
  - "Color threshold uses BEV_HARD_BLOCK_DIST_M from config to match planner hard-block logic"
  - "Circles drawn after all existing HUD elements so they are always on top"

patterns-established:
  - "BEV overlay rendering order: sidewalk fill → skeleton → paths → HUD text → obstacle circles (topmost)"

requirements-completed: [OBS-05, OBS-09]

# Metrics
duration: ~15min
completed: 2026-03-09
---

# Phase 03.1 Plan 04: BEV HUD Obstacle Visualization Summary

**draw_bev_hud() extended with metric-space obstacle circles (orange=approaching, red=stop-zone), human-verified correct BEV geometry, closing the full Phase 03.1 visual feedback loop.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-09
- **Completed:** 2026-03-09
- **Tasks:** 2 (1 auto + 1 human-verify checkpoint)
- **Files modified:** 2

## Accomplishments

- Extended `draw_bev_hud()` in `visualization.py` with `obstacle_zones_m=None` keyword parameter that renders colored exclusion circles on the BEV overlay
- Fixed latent `skel=None` crash bug in `draw_bev_hud()` (Rule 1 auto-fix, applied during Task 1)
- Wired `obstacle_zones` through `live_heading_demo.py` so the BEV panel shows live obstacle positions
- Human verified: `bev_obstacle_test.png` shows orange circle at 5m forward (near BEV top) and red circle at 0.8m forward (near BEV bottom/ego area) at correct lateral offsets
- All 69 tests remain green after visualization changes

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend draw_bev_hud() and wire in live_heading_demo.py** - `a6a7042` (feat)
2. **Task 2: Checkpoint — human-verify BEV circles** - approved by human (no code commit needed)

## Files Created/Modified

- `simulation_camera_scooter/visualization.py` - draw_bev_hud() extended with obstacle_zones_m param; skel=None crash fixed
- `simulation_camera_scooter/live_heading_demo.py` - obstacle_zones=[] initialized before predict_skip branch; draw_bev_hud called with obstacle_zones_m=obstacle_zones

## Decisions Made

- `obstacle_zones_m` defaults to `None` (not `[]`) so callers that do not supply it get zero overhead and zero drawing — the check `if obstacle_zones_m:` handles both `None` and empty list
- Color threshold tied directly to `BEV_HARD_BLOCK_DIST_M` from `config.py` so visualization matches planner hard-block logic exactly
- Circles drawn last (after all existing HUD elements) so they are always visually on top

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed skel=None crash in draw_bev_hud()**
- **Found during:** Task 1 (visualization.py extension)
- **Issue:** Existing code assumed `skel` was always a numpy array; passing `None` (as done in test verification command) raised AttributeError
- **Fix:** Added `if skel is not None:` guard around skeleton drawing block
- **Files modified:** simulation_camera_scooter/visualization.py
- **Verification:** Automated verify command passes with `skel=None`; 69 tests green
- **Committed in:** a6a7042 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug)
**Impact on plan:** Fix was required for the plan's own verify command to pass. No scope creep.

## Issues Encountered

None beyond the skel=None crash noted above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 03.1 is **fully complete**: bev_obstacle.py, realtime_nav_core integration, live_heading_demo wiring, and BEV HUD visualization are all implemented and human-verified
- Requirements OBS-01 through OBS-09 are all satisfied
- Ready for Phase 02 (BEV Recalibration) or Phase 03 Demo Integration — both depend on hardware availability
- BEV recalibration (Phase 02) remains hardware-blocked until new calibration video is recorded

---
*Phase: 03.1-yolo-bev-obstacle-projection*
*Completed: 2026-03-09*
