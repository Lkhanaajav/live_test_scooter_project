---
phase: 11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring
plan: "04"
type: execute
wave: 4
depends_on:
  - "11-03"
files_modified:
  - scripts/eval_template_planner.py
  - tests/test_template_path_planner.py
  - realtime_nav_core.py
  - live_heading_demo.py
autonomous: true
requirements:
  - TPL-03
  - TPL-04

must_haves:
  truths:
    - "Phase 11 has a replay/evaluation path that measures approval rate, low-confidence rate, fallback rate, switch rate, and heading stability"
    - "Threshold tuning is driven by explicit metrics rather than visual guesswork alone"
    - "Representative replay results can distinguish template approval success from graph fallback recovery"
  artifacts:
    - path: "scripts/eval_template_planner.py"
      provides: "offline replay/evaluation harness for Phase 11"
      contains: "approval_rate"
    - path: "tests/test_template_path_planner.py"
      provides: "replay/evaluation smoke coverage"
  key_links:
    - from: "scripts/eval_template_planner.py"
      to: "live_heading_demo.py"
      via: "shared replay and logging semantics"
      pattern: "path_source|approval_confidence|suggested_slowdown"
---

<objective>
Add the offline evaluation/tuning layer for Phase 11 and define the evidence used to accept or reject the planner on real recordings.

Purpose: tune approval thresholds against actual replay metrics and make planner behavior debuggable during thesis validation.
Output: replay/evaluation harness, metrics, and threshold-tuning guidance.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-RESEARCH.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-VALIDATION.md

@realtime_nav_core.py
@live_heading_demo.py
@scripts/train_boundary_net.py
@scripts/eval_boundary_net.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build replay/evaluation harness for template planner metrics</name>
  <files>scripts/eval_template_planner.py, tests/test_template_path_planner.py</files>
  <action>
    Add an evaluation script or harness that can replay representative planner outputs and report:
    - approval rate
    - low-confidence rate
    - fallback rate by `path_source`
    - template-family switch count
    - heading stability / spike metrics
    - occupancy or evidence collapse recovery behavior

    Keep it consistent with the repo's current logging conventions where possible.
  </action>
  <verify>
    <automated>python scripts/eval_template_planner.py --help && python -m pytest tests/test_template_path_planner.py -q -k replay</automated>
  </verify>
  <done>A replay/evaluation harness exists and reports Phase 11 acceptance metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Tune thresholds and document runtime acceptance criteria</name>
  <files>realtime_nav_core.py, live_heading_demo.py, scripts/eval_template_planner.py</files>
  <action>
    Use representative replays to set and document the first accepted values for:
    - approval threshold
    - approval margin threshold
    - minimum containment / near-field support
    - slowdown behavior under low confidence

    Keep changes minimal and evidence-driven. Do not expand scope into a new architecture here.
  </action>
  <verify>
    <automated>python -m pytest tests -q</automated>
  </verify>
  <done>Phase 11 threshold values are measurable, documented in code/comments/log outputs, and supported by replay metrics.</done>
</task>

</tasks>

<verification>
1. `python scripts/eval_template_planner.py --help`
2. `python -m pytest tests/test_template_path_planner.py -q`
3. `python -m pytest tests -q`
</verification>

<success_criteria>
- TPL-03: replay metrics show approved paths remain inside representative corridors with fewer false turn commitments
- TPL-04: low-confidence and slowdown/hold behavior are explicit, measurable, and tunable
- Phase acceptance is based on replay evidence, not only visual inspection
</success_criteria>

<output>
After completion, create `.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-04-SUMMARY.md`
</output>
