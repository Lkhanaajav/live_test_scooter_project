---
phase: 11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - template_path_planner.py
  - tests/test_template_path_planner.py
autonomous: true
requirements:
  - TPL-01
  - TPL-02
  - TPL-04

must_haves:
  truths:
    - "A fixed bank of ego-anchored smooth candidate paths exists for straight, gentle-turn, medium-turn, and sharper-turn behaviors"
    - "Templates are scored against corridor containment, edge clearance, center preference, continuity, curvature feasibility, and obstacle overlap"
    - "Approval uses both an absolute score threshold and a best-vs-second-best margin threshold"
    - "When approval fails, the planner emits low confidence and slowdown/hold guidance instead of forcing a turn"
  artifacts:
    - path: "template_path_planner.py"
      provides: "template generator, scorer, approval logic"
      contains: "generate_template_bank"
    - path: "tests/test_template_path_planner.py"
      provides: "template-bank and approval tests"
      min_lines: 180
  key_links:
    - from: "template_path_planner.py"
      to: "boundary_inference.py"
      via: "shared confidence and slowdown semantics"
      pattern: "suggested_slowdown|confidence|low_confidence"
---

<objective>
Implement the actual template planner core offline, without touching the live loop yet.

Purpose: prove that a small fixed bank of smooth trajectories can be scored and approved against corridor evidence with explicit low-confidence behavior.
Output: template bank, scorer, approval logic, and green offline tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-CONTEXT.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-RESEARCH.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-VALIDATION.md

@template_path_planner.py
@boundary_inference.py
@realtime_nav_core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add template-bank and approval tests</name>
  <files>tests/test_template_path_planner.py</files>
  <action>
    Extend Phase 11 tests to cover:
    - template bank contains straight/left/right families with bounded count
    - templates stay ego-anchored and sample into metric polylines
    - curvature filtering rejects impossible templates
    - containment and edge-clearance scoring prefer valid in-corridor templates
    - continuity favors the previous approved family when evidence is similar
    - low-confidence and slowdown trigger when approval threshold or score margin fails
    - obstacle overlap penalizes otherwise-valid templates
  </action>
  <verify>
    <automated>python -m pytest tests/test_template_path_planner.py -q -k "template or approval"</automated>
  </verify>
  <done>Phase 11 tests explicitly define the template approval contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement template generation, scoring, and approval</name>
  <files>template_path_planner.py, tests/test_template_path_planner.py</files>
  <action>
    Add the offline planner core:
    - ego-anchored cubic `y(x)` template bank with stable template ids/families
    - scoring terms for containment, clearance, center alignment, continuity, curvature feasibility, progress, obstacle penalty, and evidence support
    - approval rule with absolute threshold plus runner-up margin
    - low-confidence outputs including `confidence`, `is_low_confidence`, and `suggested_slowdown`

    Keep the scoring module lightweight and CPU-friendly. Do not integrate it into `BEVPathExtractor.process()` yet.
  </action>
  <verify>
    <automated>python -m pytest tests/test_template_path_planner.py -q && python -m pytest tests -q</automated>
  </verify>
  <done>The repo has a working offline template planner core with explicit approval and low-confidence semantics.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_template_path_planner.py -q`
2. `python -m pytest tests -q`
</verification>

<success_criteria>
- TPL-01: fixed template bank exists with controller-feasible geometry
- TPL-02: score terms are present and exercised by automated tests
- TPL-04: failed approval emits low confidence and slowdown/hold guidance
</success_criteria>

<output>
After completion, create `.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-02-SUMMARY.md`
</output>
