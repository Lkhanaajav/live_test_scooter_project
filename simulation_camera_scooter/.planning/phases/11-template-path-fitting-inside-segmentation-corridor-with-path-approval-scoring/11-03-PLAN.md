---
phase: 11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring
plan: "03"
type: execute
wave: 3
depends_on:
  - "11-02"
files_modified:
  - realtime_nav_core.py
  - live_heading_demo.py
  - template_path_planner.py
  - tests/test_template_path_planner.py
autonomous: true
requirements:
  - TPL-02
  - TPL-03
  - TPL-04

must_haves:
  truths:
    - "Intent-conditioned template approval becomes the primary selector inside BEVPathExtractor.process() before graph fallback"
    - "PathPlanResult remains compatible with the current controller and overlay code"
    - "Planner diagnostics expose approval confidence, score margin, selected template id/family, commanded intent, and whether fallback was used"
    - "live_heading_demo.py passes route/GPS intent into the planner and consumes planner confidence/slowdown when computing live speed behavior"
  artifacts:
    - path: "realtime_nav_core.py"
      provides: "Phase 11 runtime integration and PathPlanResult diagnostics"
      contains: "template"
    - path: "live_heading_demo.py"
      provides: "confidence-aware speed handling and logging"
      contains: "suggested_slowdown"
  key_links:
    - from: "realtime_nav_core.py"
      to: "template_path_planner.py"
      via: "primary template approval path inside process()"
      pattern: "template"
    - from: "live_heading_demo.py"
      to: "realtime_nav_core.PathPlanResult"
      via: "confidence-aware speed and logging updates"
      pattern: "path_source|suggested_slowdown|approval_confidence"
---

<objective>
Integrate intent-conditioned template approval into the runtime planner while preserving the controller contract.

Purpose: make intent-conditioned template approval the default path selector, keep graph search as fallback, and expose enough diagnostics and confidence behavior for the live loop to use safely.
Output: integrated runtime planner, GPS-intent-aware live-loop behavior, and green integration tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-CONTEXT.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-RESEARCH.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-VALIDATION.md

@realtime_nav_core.py
@live_heading_demo.py
@template_path_planner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime integration tests for template-first planning</name>
  <files>tests/test_template_path_planner.py</files>
  <action>
    Add integration tests that verify:
    - `BEVPathExtractor.process()` can approve an intent-consistent template path from a synthetic BEV corridor without requiring graph success
    - the approved result still yields a valid `path_model`
    - runtime diagnostics identify template vs fallback source and record commanded intent
    - low-confidence outputs propagate when corridor evidence is weak or commanded intent is unsupported
    - live-loop speed logic can consume planner slowdown/confidence without crashing
  </action>
  <verify>
    <automated>python -m pytest tests/test_template_path_planner.py -q -k integration</automated>
  </verify>
  <done>Runtime integration tests define the template-first contract before code changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate template approval into the planner and live loop</name>
  <files>realtime_nav_core.py, live_heading_demo.py, template_path_planner.py, tests/test_template_path_planner.py</files>
  <action>
    Update `BEVPathExtractor.process()` to:
    - accept maneuver intent from GPS/route logic
    - derive a corridor from the cleaned mask
    - run intent-conditioned template approval before graph search
    - keep graph/fallback logic as recovery only
    - fill `PathPlanResult` with confidence, slowdown, approval score, margin, and template identifiers

    Update `live_heading_demo.py` to:
    - pass route/GPS intent into the planner
    - log the new planner diagnostics
    - use planner slowdown/low-confidence in speed limiting rather than relying only on `has_path`
    - preserve current overlay/controller behavior
  </action>
  <verify>
    <automated>python -m pytest tests/test_template_path_planner.py -q && python -m pytest tests -q</automated>
  </verify>
  <done>Template approval is the primary runtime selector, fallbacks remain available, and the live loop consumes planner confidence.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_template_path_planner.py -q`
2. `python -m pytest tests -q`
</verification>

<success_criteria>
- TPL-03: approved runtime path stays inside representative corridors for the commanded maneuver and integrates with the existing controller contract
- TPL-04: low-confidence behavior is available to the live loop for slowdown/hold decisions when commanded intent is not yet visually supportable
- Fallbacks remain visible via diagnostics instead of silently masking planner failure
</success_criteria>

<output>
After completion, create `.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-03-SUMMARY.md`
</output>
