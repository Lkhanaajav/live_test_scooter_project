---
phase: 11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - template_path_planner.py
  - tests/test_template_path_planner.py
  - tests/conftest.py
autonomous: true
requirements:
  - TPL-01
  - TPL-02

must_haves:
  truths:
    - "A reusable corridor abstraction can be derived from cleaned BEV masks without requiring graph extraction"
    - "Corridor rows expose left/right bounds, center, width, valid-row mask, near-field support, and confidence-oriented summary fields"
    - "Synthetic corridor tests cover straight, turning, fragmented, and false-pocket cases"
  artifacts:
    - path: "template_path_planner.py"
      provides: "corridor dataclasses and mask-to-corridor extraction helpers"
      contains: "corridor_from_mask"
    - path: "tests/test_template_path_planner.py"
      provides: "Phase 11 synthetic corridor/unit tests"
      min_lines: 120
  key_links:
    - from: "template_path_planner.py"
      to: "realtime_nav_core.py"
      via: "shared BEV metric conventions and path sampling assumptions"
      pattern: "bev_forward_m|bev_lateral_m|path_sample_ds_m"
---

<objective>
Create the corridor abstraction that Phase 11 will score templates against.

Purpose: remove direct dependence on skeleton/graph geometry by converting the cleaned BEV sidewalk mask into a stable corridor representation with confidence-oriented summary fields.
Output: mask-to-corridor helper(s), synthetic fixtures, and green unit tests for corridor extraction behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-CONTEXT.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-RESEARCH.md
@.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-VALIDATION.md

@realtime_nav_core.py
@boundary_inference.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 1 corridor fixtures and failing tests</name>
  <files>tests/test_template_path_planner.py, tests/conftest.py</files>
  <action>
    Create the synthetic validation scaffold for Phase 11. Add corridor fixtures or inline generators covering:
    - straight centered corridor
    - gentle left bend
    - gentle right bend
    - fragmented near-field corridor
    - false side pocket near ego

    Write failing tests for:
    - left/right/center extraction from mask rows
    - valid-row ratio and near-field valid count
    - width consistency / width CV behavior
    - fragmentation lowering confidence-oriented summary values
    - false side pocket not dominating corridor center near ego

    Stubs may initially fail, but they must import cleanly and target the intended public API.
  </action>
  <verify>
    <automated>python -m pytest tests/test_template_path_planner.py -q -k corridor</automated>
  </verify>
  <done>Phase 11 corridor tests exist and exercise the intended public corridor contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement mask-to-corridor extraction and summaries</name>
  <files>template_path_planner.py, tests/test_template_path_planner.py</files>
  <action>
    Implement a reusable corridor module with:
    - row-wise corridor dataclass/structure
    - `corridor_from_mask(...)` that derives left/right boundary, center, width, valid mask, and forward ordering from a cleaned BEV mask
    - summary metrics including `valid_ratio`, `near_field_valid_count`, `width_cv`, `forward_span_m`, and an evidence/confidence summary

    Reuse the repo's current BEV metric conventions and near-field sanity ideas from `boundary_inference.py`.
    Keep the module independent from graph search logic.
  </action>
  <verify>
    <automated>python -m pytest tests/test_template_path_planner.py -q -k corridor && python -m pytest tests -q</automated>
  </verify>
  <done>`corridor_from_mask(...)` returns a stable corridor contract and all Wave 1 corridor tests are green.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_template_path_planner.py -q -k corridor`
2. `python -m pytest tests -q`
</verification>

<success_criteria>
- TPL-01: corridor geometry is available in the same BEV metric frame used by the controller
- TPL-02: the planner has the evidence fields needed for later template scoring and approval
- Synthetic corridor fixtures cover straight, turning, fragmented, and false-pocket cases
</success_criteria>

<output>
After completion, create `.planning/phases/11-template-path-fitting-inside-segmentation-corridor-with-path-approval-scoring/11-01-SUMMARY.md`
</output>
